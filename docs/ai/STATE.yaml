{
    "schema_version":  1,
    "ticket":  "TNX-P9B-PRESENTATION-ASSETS",
    "title":  "Phase 9B - PKCERT presentation and final demo assets",
    "status":  "complete",
    "owner":  "unassigned",
    "reviewer":  "codex",
    "branch":  "feat/phase-9b-presentation-assets",
    "worktree":  "C:\\Users\\LENOVO\\Desktop\\ThreatNeXus",
    "starting_commit":  "f220351",
    "goal":  "Create a professional PKCERT-ready presentation package: deck source + speaker notes, a demo walkthrough, a screenshot plan, a style guide with inspiration notes, a landing-page plan, and a README visual-tour link. Presentation and demo assets only - no product features, no code changes.",
    "scope":  {
                  "included":  [
                                   "docs/presentation/ThreatNeXus-PKCERT-Deck.md - 16-slide deck source (short slide text)",
                                   "docs/presentation/ThreatNeXus-PKCERT-Deck.pptx - the generated deck, built with pptxgenjs, validated clean (schema/relationship/content-type checks passed), speaker notes embedded on every slide",
                                   "docs/presentation/SPEAKER_NOTES.md - per-slide: what to say, what visual/screenshot to show, what not to overclaim, demo cue, offline/provider-outage fallback",
                                   "docs/presentation/DEMO_WALKTHROUGH.md - pre-demo checklist, stack prep, role login order, and dedicated provider-enrichment/AI-assistance/ATT&CK-mapping walkthrough sections plus mid-demo provider-outage recovery, none of which docs/DEMO_SCRIPT.md covers",
                                   "docs/presentation/SCREENSHOT_PLAN.md - 9 exact screenshot slots (P-01..P-09) with filenames, roles, and capture instructions; none captured this phase (no local stack was running - Docker daemon unreachable, Docker Desktop not found at its standard path, and starting a debug session to force one up was correctly out of scope per instruction)",
                                   "docs/presentation/STYLE_GUIDE.md - deck strategy (audience/hook/pain/promise/proof-points/demo-sequence/overclaim-guardrails), inspiration notes from a short real research pass (Falco/Trivy/Sigstore-family README structure via WebFetch, general cybersecurity pitch-deck structure via WebSearch - structure only, nothing copied), visual rules (reusing the product's OWN design tokens as the palette), the full banned-overclaim-word list, AI/provider/security phrasing guardrails, PKCERT-only framing checklist, no-generic-AI-writing quality checklist",
                                   "docs/presentation/LANDING_PAGE_PLAN.md - planning only, explicitly not built: worth-building assessment, problem it would solve, where it would live, sections, screenshot needs, why it stays optional, the trigger condition to revisit it",
                                   "docs/DEMO_SCRIPT.md lightly extended with a cross-link to the new DEMO_WALKTHROUGH.md",
                                   "README.md lightly updated: added a Phase 9B status-table row, added deck/walkthrough links to the Documentation table, replaced the old 'Phase 9B addition' screenshot placeholder note with a link to the real SCREENSHOT_PLAN.md, updated the Roadmap section (landing page + real screenshots now the stated remaining items instead of a vague '9B' placeholder), updated the 'Current status' heading and its self-referencing anchor link to Phase 9B",
                                   "docs/ai/STATE.yaml and docs/ai/HANDOFF.md updated for Phase 9B closure (this checkpoint)"
                               ],
                  "excluded":  [
                                   "Any new product feature, provider, or code change beyond the trivial cross-link edits to docs/DEMO_SCRIPT.md and README.md listed above",
                                   "A built landing/showcase page - LANDING_PAGE_PLAN.md is planning only, per explicit hard scope",
                                   "PR creation or merge into main",
                                   "Actual screenshots - no local stack was running; SCREENSHOT_PLAN.md documents exactly what to capture and how",
                                   "docs/presentation/PPTX_BUILD_NOTES.md - not created, because a real .pptx WAS generated successfully this session (the file only needed to exist if PPTX generation wasn't possible)"
                               ]
              },
    "completed":  [
                      "Mandatory start gate: fetched origin. PR #17 (Phase 9A) was found OPEN, NOT MERGED, at the time this ticket started - this contradicts the start-gate's assumption that it was already merged. Reported honestly rather than proceeding as if merged: branched feat/phase-9b-presentation-assets from the tip of feat/phase-9a-professional-docs (f220351, the same commit content PR #17 would merge) instead of from main, since that branch already carries every Phase 9A source document this ticket needed to read. This branch should be rebased onto main once PR #17 actually merges, before its own PR is opened.",
                      "Activated the AI development-team workflow per explicit user instruction; read global AGENTS.md/CODEX.md/GLOBAL-MEMORY.md (unchanged since prior phases this session), README.md, and every Phase 9A doc listed in the instruction (all already in this session's own context, having been authored in the immediately preceding Phase 9A turn) plus docs/ai/STATE.yaml/HANDOFF.md/SECURITY.md",
                      "No writer lock file exists in this repo (.ai-team/ absent) - same known F-drive start-task.ps1 schema-mismatch gap as every phase since 8B; proceeded under the documented workaround",
                      "Ran a short, real research pass before drafting: WebSearch for open-source security-tool README conventions and general cybersecurity pitch-deck structure, then WebFetch on github.com/falcosecurity/falco specifically asking for STRUCTURE ONLY (section order and purpose, not wording) - recorded as inspiration notes in STYLE_GUIDE.md with the actual real project named, no star-count claims made anywhere (none verified live, so none stated), nothing copied",
                      "Wrote an internal deck strategy (audience/hook/pain/promise/proof-points/demo-sequence/overclaim-guardrails) into STYLE_GUIDE.md before writing any slide content, per instruction",
                      "Wrote all slide content and speaker notes grounded in Phase 9A's own documented facts (role/capability matrix, six-provider stack, AI governance rules, security controls, architecture, 23 migrations/145 test files/9 evaluators/9 e2e specs) - no new facts invented, no numbers not already verified in Phase 9A",
                      "Chose the product's own real design tokens (frontend/src/theme/tokens.js: near-black canvas #0A1210, government-green accent #35C477) as the deck palette rather than inventing an unrelated one, for brand consistency with the actual running application",
                      "Attempted to bring up the local stack for real screenshots: Docker daemon unreachable (npipe connection failed), Docker Desktop executable not found at its standard install path - confirmed via two quick checks, did not escalate into a longer debug session per the explicit 'do not start a long debug session just for screenshots unless explicitly approved' instruction. Documented every screenshot slot as an exact, capturable placeholder instead.",
                      "Generated the .pptx with pptxgenjs (installed locally in the session scratchpad after confirming it was not globally preinstalled on this Windows machine - the skill's 'preinstalled' assumption did not hold in this environment, so installed it per the skill's own documented fallback)",
                      "Ran the pptx skill's schema/relationship/content-type validator (scripts/office/validate.py) - PASSED clean on the first run once a Windows console-encoding issue (PYTHONUTF8=1 needed for the unicode punctuation used in two slides) was worked around; this was an environment quirk in the validator's own console handling, not a defect in the generated file",
                      "Attempted the skill's required image-based visual QA step (soffice.py --convert-to pdf, then pdftoppm): LibreOffice is not installed on this Windows machine at all (no soffice binary found), and the skill's soffice.py wrapper itself assumes a Linux sandbox (uses AF_UNIX sockets, which don't exist on Windows) so it cannot run here regardless. No Microsoft Office/PowerPoint installation was found either. Visual (rendered-image) QA could not be performed in this environment - stated as an honest limitation, not skipped silently. Substituted a full content QA pass instead: dumped every slide's text and notes via python-pptx and cross-checked it against the deck source markdown (all 16 slides present, correct order, no placeholder/lorem text, matches source) and manually verified box/text-box coordinate math for every slide against the 13.333x7.5 canvas to check for out-of-bounds or overlapping placement.",
                      "Lightly extended docs/DEMO_SCRIPT.md and README.md with cross-links to the new presentation package, keeping both edits small per the 'avoid bloating the README' instruction",
                      "Updated docs/ai/STATE.yaml and docs/ai/HANDOFF.md for Phase 9B closure"
                  ],
    "validation":  {
                       "git_status_before":  "Clean on feat/phase-9b-presentation-assets, branched from f220351 (Phase 9A's tip - see the PR #17 note above for why this is not literally 'main')",
                       "git_status_after":  "docs/presentation/ (new, 7 files + assets/README.md placeholder), README.md modified, docs/DEMO_SCRIPT.md modified. No other file touched. docs/codex/ untouched. No backend/frontend code changed.",
                       "pptx_schema_validation":  "PASS - scripts/office/validate.py: 'All validations PASSED!' (required PYTHONUTF8=1 to work around a Windows console-encoding quirk in the validator itself, not a file defect)",
                       "pptx_content_qa":  "PASS - full text+notes dump via python-pptx cross-checked against the deck source; all 16 slides present in order; no placeholder/lorem/TODO text found by markitdown's own grep pattern",
                       "pptx_visual_qa":  "NOT PERFORMED - no LibreOffice or Microsoft Office installed in this environment, and the skill's own soffice.py wrapper requires AF_UNIX sockets unavailable on native Windows. Stated honestly as a limitation rather than skipped silently; manual coordinate-math verification substituted where possible.",
                       "secret_sweep":  "PASS - git-grep with CI's own credential-shaped-literal patterns found nothing in any new/changed file, including the binary .pptx (grepped as text; no match)",
                       "no_real_env_read":  "PASS - no .env file of any kind was read, printed, or referenced this phase",
                       "link_check":  "PASS - every relative markdown link added or changed this phase resolves to a real file",
                       "code_changes":  "NONE beyond the two small documentation cross-link edits (README.md, docs/DEMO_SCRIPT.md) - no backend/frontend test run was needed or performed",
                       "ci":  "PENDING - see next_action"
                   },
    "known_issues":  [
                         "PR #17 (Phase 9A) is OPEN, not merged, as of this ticket's start - this branch is based on its tip (f220351) rather than an updated main. Rebase onto main once #17 merges, before opening this ticket's own PR.",
                         "No screenshots were captured - SCREENSHOT_PLAN.md documents exactly what's needed; capture them the next time the local stack is actually running.",
                         "Visual (rendered-image) QA of the .pptx could not be performed in this environment (no LibreOffice/Office installed) - the file passed strict schema/content-type/relationship validation and a full content-text QA pass, but nobody has looked at a rendered image of it yet. Recommend a quick open-in-PowerPoint sanity check before the first real presentation.",
                         "F-drive start-task.ps1 throws against this repo's STATE.yaml schema; no working .ai-team/WRITER_LOCK.json mechanism exists for this repo. Worked around, not fixed - same known gap as every phase since 8B.",
                         "docs/codex/ remains untracked and untouched, per the one-writer boundary.",
                         "No independent reviewer (Codex or otherwise) has looked at this deliverable yet - per CLAUDE.md 'do not review your own final work as the only reviewer', an independent pass is recommended, and is especially valuable here given the visual-QA gap above."
                     ],
    "next_action":  "Commit the diff (explicit paths only), push feat/phase-9b-presentation-assets, and watch CI to green before releasing the writer lock. Once PR #17 merges, rebase this branch onto main before opening its own PR. Recommended: merge Phase 9A, then this ticket, then a Codex/independent pass and a manual open-in-PowerPoint visual check, then final rehearsal.",
    "checkpoint_ref":  "pending-commit",
    "updated_at":  "2026-08-09T21:30:00.0000000Z",
    "next_writer":  "unassigned"
}
