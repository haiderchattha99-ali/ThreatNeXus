{
    "schema_version":  1,
    "ticket":  "TNX-P10A1-ENRICHMENT-ORCHESTRATION-FOUNDATION",
    "title":  "Phase 10A-1 - inert enrichment-orchestration foundation (Codex review passes 1 and 2 resolved)",
    "status":  "handoff_ready",
    "owner":  "unassigned",
    "reviewer":  "codex",
    "branch":  "feat/phase-10a1-enrichment-orchestration-foundation",
    "worktree":  "F:\\AI-Worktrees\\ThreatNeXus\\phase-10a1",
    "starting_commit":  "5c0de84",
    "goal":  "Deliver the inert Phase 10A-1 enrichment-orchestration foundation - one additive migration (5 tables, 9 enums, 7 CHECK constraints), subject model, applicability router, run/item/job services, reconciliation service (callable, unscheduled), async run + summary + usage APIs, default-off configuration, ingestion integration and backend tests - and resolve every blocker from BOTH Codex independent reviews. Zero provider calls, zero quota reservations, zero attempt/usage rows, no worker, no frontend change.",
    "binding_contract":  "docs/ai/PHASE-10A1-API-CONTRACT.md - Git-tracked, written BEFORE this correction pass touched code. Routes, request/response shapes, status codes, closed vocabularies and the upload block\u0027s exact key set. Code that disagrees with it is wrong; amending it needs explicit approval plus a DECISIONS.md entry. It exists because the Phase 10 Plan v2/v2.1 documents are on no disk in this project and two sessions running reconstructed the surface differently.",
    "scope":  {
                  "included":  [
                                   "One additive migration: 5 tables, 9 enums, 7 CHECK constraints. Still 24 migrations - the unmerged Phase-10 migration was AMENDED with one column rather than followed by a 25th, so CI\u0027s frozen list is unchanged",
                                   "EnrichmentSubjectType + provider/subject compatibility (5 IPv4 providers, nvd CVE-only) + a provider purpose map, canonicalization before hashing",
                                   "queryIdentityHash (work identity) kept SEPARATE from requestScopeHash (ask identity); bounded Idempotency-Key handling (max 128 UTF-8 bytes, no control chars, SHA-256 digest only)",
                                   "Applicability router with explicit precedence; force bypasses freshness only",
                                   "Run/item/lookup-job repository + services, run-state recomputation, shared activeLookupKey dedup",
                                   "CONVERGENT item materialization returning per-call itemsCreated / jobsCreated / jobsShared / skipped counts",
                                   "Delegated providers (abuseipdb, nvd) scheduled through the CANONICAL queue services and linked; no RUN_DELEGATED job is ever created without exactly one delegate FK",
                                   "Reconciliation service - callable in tests, scheduled by nothing",
                                   "THREE Finding-scoped routes: POST /api/findings/:id/enrichment/runs, GET .../runs/:runId, GET /api/findings/:id/enrichment/summary - plus the pre-existing ADMIN usage API",
                                   "AUTO_ENRICHMENT_ENABLED=false, ENRICHMENT_WORKER_ENABLED=false, every automatic budget 0, manual budgets unlimited-but-validated",
                                   "Ingestion integration behind the switch + one additive six-key `enrichment` response block, surfaced over HTTP by the upload controller",
                                   "Audit events for the inert milestone (counts, closed codes, and a bounded justification preview only)"
                               ],
                  "excluded":  [
                                   "worker.js or any worker loop; provider executors, factories, lookups, fetch calls - a static inertness test proves the package imports none of them",
                                   "Quota reservation SQL/service; ProviderLookupAttempt rows; ProviderDailyUsage rows - all zero by design",
                                   "MANUAL_DIRECT wiring; targeted IOC runner changes; vulnerability runner changes; automatic NVD/EPSS/KEV execution",
                                   "A run-LIST surface. It was unplanned, was never part of the binding contract, and has been REMOVED - the summary endpoint answers the question it stood in for",
                                   "Frontend changes (git status frontend/ empty), cancellation, Risk v1 changes, live provider smoke tests",
                                   "PR creation and any merge to main - explicitly out of scope",
                                   "backend/.env - never opened, read, printed or referenced; only .env.example consulted",
                                   "The primary checkout C:\\Users\\LENOVO\\Desktop\\ThreatNeXus and its Phase 9 documentation/presentation work - never touched, never switched, never stashed",
                                   "docs/codex/ - not present in this worktree and never created"
                               ]
              },
    "codex_review_pass_2":  {
                                "verdict_received":  "NOT READY - five findings",
                                "status":  "all five resolved on this branch, each with a regression test",
                                "findings":  [
                                                 "1. Missing summary endpoint. ADDED GET /api/findings/:id/enrichment/summary behind read:findings: one timestamped row per known provider (always all six, stable order), closed status (NO_SUBJECT / NOT_REQUESTED / PENDING / COMPLETED / UNAVAILABLE / SKIPPED), purpose (IOC_REPUTATION / EXPOSURE / VULNERABILITY) and source (NONE / ORCHESTRATION_JOB / IOC_ENRICHMENT / VULNERABILITY_ENRICHMENT) vocabularies, plus skipReason / asOf / freshUntil / isStale / evidenceAvailable. Delegated providers resolve state from the DELEGATE row, not from the job\u0027s WAITING_ON_DELEGATE. NVD carries per-CVE subrows; a Finding with no ACTIVE ANALYST_VERIFIED CVE reads NO_SUBJECT + NO_SUBJECT_FOR_PROVIDER and creates NO NVD item; an IP is never an NVD subject. No provider call, no attempt row, no reservation, no write of any kind. The unplanned run-LIST surface was REMOVED rather than kept.",
                                                 "2. POST .../runs corrected. Request accepts providers / force / justification; an unknown provider is 400; justification is trimmed and bounded to 1000 chars, REQUIRED when force=true (400 otherwise), never echoed in a response, and reaches audit only as a \u003c=200-char preview - the same convention findingEnrichmentScheduleService already uses. The response now names outcome, executionState, run and items as four distinct top-level fields instead of flattening the run into `data`. CREATED=202, ALREADY_RUNNING=200 (existing run returned), SKIPPED=200. executionState is PAUSED_WORKER_DISABLED with the worker switch off and NOT_IMPLEMENTED with it on. Location: /api/findings/:id/enrichment/runs/:runId is set on every outcome. Body and header pinned in Supertest.",
                                                 "3. Upload `enrichment` block corrected to EXACTLY six keys - state, runsCreated, itemsCreated, jobsCreated, jobsShared, skipped - in every state. enabled, runsDeduplicated, failedCount and executed are gone from the public block (failedCount survives in the audit payload, where operational detail belongs). Every count describes what THAT upload wrote. Proven with exact key allow-lists disabled AND enabled, and with a real-PG case where a second Finding on one IP counts its job as SHARED, not created.",
                                                 "4. Two literal NUL bytes removed from enrichmentRunService.js. targetKey() is now JSON.stringify([provider, subjectType, subjectValue]) - text-safe and collision-resistant by construction. A new inertness case reads every file in the package as BYTES and fails on any NUL.",
                                                 "5. Historical truthfulness. consideredProviders.noSubject is now DURABLY recorded on FindingEnrichmentRun.noSubjectProviders (sorted, comma-joined, allow-listed), written once at creation and never updated. The immediate POST and any later GET return the identical list; associating a verified CVE afterwards does not change a historical run; T-09 holds (no NVD run item exists when no active analyst-verified CVE existed); nothing reverses requestScopeHash. The five-table model is UNCHANGED - one column was added to an existing table, and the unmerged Phase-10 migration was amended rather than followed by a 25th, so CI\u0027s frozen migration list still reads 24."
                                             ],
                                "design_note":  "The column stores the NO-SUBJECT set rather than the whole requested scope, because the requested scope is already recoverable as providers(items) UNION noSubjectProviders. Storing both would be two sources for one truth.",
                                "extra_defect_found_and_fixed":  "phase10a1Constraints.test.js deliberately INSERTs a \u0027p10a1c-\u0027 marked ProviderDailyUsage row to prove the reserved-count CHECK constraint. Three other Phase-10 suites asserted a GLOBAL zero on that table and therefore failed intermittently when run concurrently with it - a true claim broken by a test fixture. The global assertions now exclude that one marker and nothing else."
                            },
    "codex_review_pass_1":  {
                                "verdict_received":  "NOT READY - six blockers",
                                "status":  "all six resolved earlier on this branch (commit 4fa62e6), each with a regression test",
                                "findings":  [
                                                 "1. API contract: replaced the invented /:id/enrichment-runs with /:id/enrichment/runs, no alias retained; replaced `created: false` under 202 with the closed CREATED / ALREADY_RUNNING / SKIPPED outcome.",
                                                 "2. Upload contract: renamed the additive `autoEnrichment` block to `enrichment` with a closed state vocabulary, and surfaced it over HTTP for the first time.",
                                                 "3. Partial-materialization recovery: convergent materialization replaced the `items.length === 0` guard.",
                                                 "4. Unlinked delegated jobs: delegates are created-or-found through the canonical queue services and linked, or no job is created at all.",
                                                 "5. HTTP/RBAC contract tests added over the mounted application.",
                                                 "6. AI-team state rewritten to the current ticket only."
                                             ]
                            },
    "validation":  {
                       "prisma":  "format + validate pass; migrate deploy from zero applies 24/24 on a fresh database (tnx_p10a1_r2); migrate diff --exit-code reports no difference (exit 0)",
                       "unit":  "2416 tests across 108 files - including the static inertness gate (now covering 11 modules) and its NEW byte-level NUL-byte case, and a new 15-test summary suite",
                       "real_pg_constraints":  "9 tests proving 8 independent rejections across the 7 constraints, each asserting the constraint NAME",
                       "real_pg_orchestration":  "10 tests - the 9 prior ones plus historical truthfulness: a CVE associated AFTER a run was recorded does not change that run\u0027s stored noSubjectProviders and creates no retroactive NVD item, while a NEW ask does see the new subject",
                       "real_pg_default_off":  "4 tests - off = zero Phase-10 rows and the exact six-key AUTOMATIC_DISABLED block; on = durable records with truthful created/shared/skipped counts, jobs non-terminal and never queried, zero attempts, zero usage; a second Finding on ONE IP counts its job as SHARED not created; identical re-upload creates no second run",
                       "real_pg_delegate_recovery":  "9 tests, unchanged in intent - crash recovery, concurrent replay convergence, delegate created / reused / past-terminal, scheduling refusal, NVD delegate, three CVEs three delegates, ADMIN-batch uniqueness respected",
                       "http_rbac":  "52 Supertest tests over the mounted application: 401 unauthenticated on every route; ADMIN/ANALYST create, REVIEWER/VIEWER 403 never downgraded to 401; all four roles read both the run and the summary through read:findings; the three contract paths served and BOTH the hyphenated aliases and the removed run-list 404; 202 CREATED / 200 ALREADY_RUNNING / 200 SKIPPED with exact body keys and the exact Location header; justification validation, non-echo and audit-preview bounding; summary shape, NVD NO_SUBJECT, cross-Finding isolation and zero writes; no job id, hash, claim token or foreign subject in any body",
                       "upload_http_contract":  "reportUploadRoute.test.js pins the EXACT six-key default-off block with toEqual, so a removed field cannot silently return, and the response allow-list test pins the block\u0027s own keys",
                       "full_suite":  "3449 passed, 2 skipped, 0 failed (157 files) on a fresh database at --maxWorkers=3, with every provider key empty",
                       "evaluators":  "eval:phase1 PASS, eval:risk PASS (19/19), eval:phase2 PASS (22/112), eval:phase3 PASS (12/151), eval:vulnerability PASS (41/992), eval:phase4 PASS (14/151), eval:phase5 PASS (14/150), eval:phase6.3 PASS (13/108), eval:phase7 PASS (8/35) - run with EVAL_DATABASE_URL pointed at a separate disposable database",
                       "risk_unchanged":  "RISK_CONFIGURATION_VERSION v1.0.0 and RISK_ALGORITHM_VERSION risk-additive-bucketed-v1 unchanged; git status backend/src/services/risk/ is empty; eval:risk re-proves the fingerprint",
                       "secret_scan":  "CI\u0027s own three hygiene checks run locally over the tracked tree: no committed .env, no credential-shaped literal, no committed build output. Clean",
                       "frontend":  "untouched - git status frontend/ is empty",
                       "no_real_env_read":  "PASS - backend/.env never opened, read, printed or referenced",
                       "primary_checkout":  "untouched - C:\\Users\\LENOVO\\Desktop\\ThreatNeXus stayed on docs/phase-9c-pkcert-technical-dossier with its unrelated Phase 9 work intact",
                       "ci":  "GREEN on the first push of 9c18265 - run 31574190866 (https://github.com/haiderchattha99-ali/ThreatNeXus/actions/runs/31574190866). All six required jobs success: hygiene, schema, backend, frontend, Browser suite (Chromium), core evaluators. The seventh (mutation and concurrency gates) is manual-dispatch-only and correctly skipped"
                   },
    "known_issues":  [
                         "MANDATORY PRE-10A2 BLOCKER: the existing IOC runner owns claimPendingJob and calls provider.lookup() internally. A targeted runner/hook contract must be designed AND independently reviewed before 10A-2 - see docs/ai/HANDOFF.md for the seven guarantees it must provide.",
                         "MANUAL_DIRECT, ProviderLookupAttempt and ProviderDailyUsage are defined and constraint-proven but deliberately unused or empty in 10A-1 - they exist so the accounting contract is provable before any code can spend quota.",
                         "ENRICHMENT_WORKER_ENABLED is declared and validated but consumed by nothing except the executionState reported on run and summary responses; there is no worker to enable.",
                         "A RUN_DELEGATED job without a delegate FK is prevented in code and asserted per job in the real-PG suite, not by a database CHECK constraint - promote it when 10A-2 next touches the migration set.",
                         "The summary endpoint resolves a delegate only through a link an orchestration item actually recorded. It does NOT hunt for unlinked evidence produced by the legacy ingestion path; such a Finding reads NOT_REQUESTED / source NONE there, and that evidence is served by the pre-existing GET /api/findings/:id/enrichments surface. This boundary is deliberate and is documented in the module header and the binding contract.",
                         "VulnerabilityEnrichmentJob carries no expiry column, so an NVD delegate\u0027s freshUntil is reported as null (unknown) rather than invented.",
                         "Freshness is read one query per routed target (bounded by provider scope plus one per verified CVE). Marked with a ponytail: comment naming the batching upgrade path.",
                         "The Phase 10 Plan v2 and v2.1 documents exist in no file on disk or on the F drive. The binding contract is now docs/ai/PHASE-10A1-API-CONTRACT.md. If v2/v2.1 ever surfaces and names anything differently, reconcile the NAMES against that file.",
                         "Local full-suite runs at DEFAULT parallelism produce 5s-timeout failures in unrelated pre-existing suites. Proven unrelated. A fresh database at --maxWorkers=3 is fully green.",
                         "The THIRD independent review has not happened yet - per CLAUDE.md this must not be treated as final."
                     ],
    "next_action":  "Codex THIRD independent review of feat/phase-10a1-enrichment-orchestration-foundation against docs/ai/PHASE-10A1-API-CONTRACT.md and the five pass-2 findings in STATE.yaml. Do NOT open a PR, do NOT merge main, do NOT begin 10A-2.",
    "checkpoint_ref":  "a84d60aae8949496ad7879c0742534154cce70f9",
    "updated_at":  "2026-08-12T07:34:34.4786938Z",
    "current_work":  "",
    "files_changed":  [
                          "docs/ai/PHASE-10A1-API-CONTRACT.md (new - the binding contract)",
                          "backend/prisma/schema.prisma (FindingEnrichmentRun.noSubjectProviders)",
                          "backend/prisma/migrations/20260811162611_add_phase10a1_enrichment_orchestration/migration.sql (amended, still 24 migrations)",
                          "backend/src/services/enrichmentOrchestration/enrichmentSummaryReadService.js (new)",
                          "backend/src/services/enrichmentOrchestration/enrichmentDecisionCodes.js",
                          "backend/src/services/enrichmentOrchestration/enrichmentSubject.js",
                          "backend/src/services/enrichmentOrchestration/enrichmentRunService.js",
                          "backend/src/services/enrichmentOrchestration/enrichmentRunReadService.js",
                          "backend/src/services/enrichmentOrchestration/enrichmentOrchestrationRepository.js",
                          "backend/src/controllers/enrichmentOrchestrationController.js",
                          "backend/src/routes/enrichmentOrchestrationRoutes.js",
                          "backend/src/services/ingestion/reportIngestionService.js",
                          "backend/tests/unit/enrichmentSummaryReadService.test.js (new)",
                          "backend/tests/unit/enrichmentOrchestrationInertness.test.js",
                          "backend/tests/unit/enrichmentRunServices.test.js",
                          "backend/tests/integration/phase10a1RouteAuthorization.test.js",
                          "backend/tests/integration/phase10a1Orchestration.test.js",
                          "backend/tests/integration/phase10a1IngestionDefaultOff.test.js",
                          "backend/tests/integration/phase10a1DelegateRecovery.test.js",
                          "backend/tests/integration/reportUploadRoute.test.js",
                          "README.md, docs/ai/HANDOFF.md, docs/ai/STATE.yaml"
                      ],
    "next_writer":  "codex"
}
