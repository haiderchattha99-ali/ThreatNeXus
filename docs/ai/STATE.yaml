{
    "schema_version":  1,
    "ticket":  "TNX-P8C-AI-ASSISTANCE-MVP",
    "title":  "Phase 8C - AI Assistance MVP (analyst-assist suggestions, disabled by default, human-approved only)",
    "status":  "complete",
    "owner":  "unassigned",
    "reviewer":  "codex",
    "branch":  "feat/phase-8c-ai-assistance-mvp",
    "worktree":  "C:\\Users\\LENOVO\\Desktop\\ThreatNeXus",
    "starting_commit":  "19bf6e6",
    "goal":  "Build analyst-assist AI suggestions (finding summary draft, analyst explanation draft) that are disabled by default and human-approved only. AI must never make final decisions, never automatically close findings, never approve mappings, never notify organizations, never mutate authoritative security state without explicit analyst action. Reuse the existing provider/config/audit/rate-limit foundation (Phases 2/6/7/8A/8B) and the Phase 5 AI foundation's shape rather than rebuilding either.",
    "scope":  {
                  "included":  [
                                   "AISuggestion Prisma model (FindingAiSuggestion) + additive-only migration 20260807100000_add_phase8c_finding_ai_suggestions (own table, no change to any Phase 5 AI table)",
                                   "services/aiAssist/: aiAssistRules.js, aiAssistProvider.js (contract + disabled provider), mockAiAssistProvider.js, aiAssistProviderRegistry.js, aiAssistRuntime.js (reuses env.AI_ENABLED/env.AI_PROVIDER - ONE operator switch for both AI domains), findingEvidenceSnapshot.js (bounded per-Finding snapshot, reuses caseEvidenceSnapshot.js's canonicalize/fingerprint helpers), findingAiSuggestionService.js (request/list), findingAiSuggestionDecisionService.js (accept/reject, staleness -> EXPIRED on accept only), aiAssistSerializers.js",
                                   "backend/src/controllers/aiFindingSuggestionController.js + backend/src/routes/aiFindingSuggestionRoutes.js, mounted at /api/findings (GET/POST .../ai-suggestions, POST .../accept, POST .../reject)",
                                   "Two new capabilities (read:ai-finding-suggestions, request:ai-finding-suggestions: ADMIN+ANALYST); decide REUSES the pre-existing Phase-0 review:ai-suggestions grant (ADMIN+REVIEWER) instead of minting a third - genuine separation of duties (ANALYST drafts, REVIEWER/ADMIN decides), and finally wires up a capability that had sat unused since Phase 0",
                                   "Audit events: ai.suggestion.requested/generated/failed/accepted/rejected, ai.unavailable - no secrets, no raw prompts/proposedText/snapshot in audit rows, only closed reason codes and lengths",
                                   "Shares the SAME providerRateLimiter budget every other provider-execution route draws on (IOC/CVE/Censys enrichment, AI mapping suggestions) - proven in phase7RateLimiting.test.js",
                                   "51 new/updated tests: aiAssistRules.test.js (13), aiAssistSafetyBoundaries.test.js (9, structural+behavioural provider-safety proof), findingAiPromptInjection.test.js (4), phase8cFindingAiAssistanceEvidence.test.js (9), aiFindingSuggestionRouteAuthorization.test.js (13, full HTTP capability matrix), 2 new phase7RateLimiting.test.js cases, 1 roles.test.js length-formula update. Full backend suite: 2950 passed / 177 skipped (up from the 2899 baseline by exactly the tests this ticket adds, zero regressions)",
                                   "Docs: README.md (AI finding assistance provider table row), docs/ai/SECURITY.md (new Phase 8C section), docs/ai/HANDOFF.md, docs/ai/STATE.yaml",
                                   "Sequencing fix: fast-forwarded local main (4fb373c -> 19bf6e6) to pick up Phase 8B's already-merged-into-main state before branching; branched feat/phase-8c-ai-assistance-mvp from the updated main; acquired the AI-team writer lock directly (F-drive start-task.ps1 has a schema bug against this repo's STATE.yaml shape - worked around, not fixed, flagged as a known tooling gap); registered the new migration in CI's frozen migration-history list"
                               ],
                  "excluded":  [
                                   "Automatic notification sending, automatic finding closure, automatic ATT&CK approval, automatic remediation decisions - none implemented, none possible: the provider contract has one method and returns data only",
                                   "Autonomous agent workflows, background scheduled AI jobs, queues/schedulers",
                                   "A live AI provider of any kind - none exists in this repository (same Phase 5 boundary); no new API key, no new .env.example entry (AI_ENABLED/AI_PROVIDER already existed)",
                                   "A live AI smoke script - there is no live provider to smoke-test in this milestone, unlike NVD/Censys",
                                   "Shadowserver, Shodan, Netlas, GreyNoise, VirusTotal, OTX, MISP integration",
                                   "Frontend surface - deferred to Phase 8C.1 per this ticket's own explicit fallback clause; backend/API/tests/docs shipped first",
                                   "PR creation or merge into main"
                               ]
              },
    "completed":  [
                      "Verified the mandatory start gate: fetched origin, found Phase 8B already merged into origin/main via PR #11 (19bf6e6) while local main was 6 commits stale; fast-forwarded local main, branched from the updated tip",
                      "Read the FULL existing Phase 5 AI foundation (services/ai/: aiProviderRegistry, aiRuntime, aiMappingProvider, mockAiMappingProvider, aiSuggestionService, aiSuggestionRules, aiSuggestionDecisionService, caseEvidenceSnapshot, aiAssistanceRoutes, frameworkMappingController's AI handlers) before writing anything, to avoid rebuilding an already-shipped, already-tested AI safety foundation",
                      "Designed Phase 8C as a deliberately SMALLER, parallel AI domain (Finding-level narrative drafts) rather than extending Phase 5's mapping-suggestion contract - own registry/provider contract/runtime (generateSuggestion vs suggestMappings), own table, no run/suggestion split needed since one request produces one row",
                      "Reused rather than rebuilt: env.AI_ENABLED/env.AI_PROVIDER (same switch), providerRateLimiter (same budget), auditService/buildAuditContext, runAtomic/runSerializable, assertPositiveInteger/assertValidDate from frameworkMappingRules.js, caseEvidenceSnapshot.js's canonicalize/fingerprint helpers, the shared tests/unit/fixtures/workflowPrismaFake.js (extended additively with one table binding rather than duplicated)",
                      "Discovered and reused the dormant Phase-0 review:ai-suggestions capability for accept/reject - it was drafted for a review-of-somebody-else's-work model that did not fit Phase 5's mapping-suggestion decide (which needs mapping-write authority) but fits this ticket exactly (accepting a draft writes nothing but its own review state)",
                      "Prisma: FindingAiSuggestion model + 2 enums, additive-only migration, hand-authored mirroring the exact FK/index/enum SQL shape of the Phase 5 and Phase 8B migrations (verified via prisma validate + prisma generate; Docker Desktop's daemon never came up in this sandbox, so migrate-deploy-from-zero against a real disposable Postgres could not be run locally - see known_issues)",
                      "Implemented the full backend surface: provider abstraction, runtime, evidence snapshot, suggestion service, decision service, controller, routes, capability wiring, app.js mounting (before findingReadRoutes, same ordering constraint as censysEnrichmentRoutes)",
                      "Wrote and iterated 51 new tests; fixed 2 genuine test-authoring bugs found by running them (a route test wrongly assumed AI_PROVIDER=mock is reachable via env alone - it correctly is not, by design; a safety-boundary test wrongly expected hostile extra provider-result fields to be explicitly rejected rather than silently excluded by construction, which is the stronger and correct behaviour) and 1 pre-existing test (roles.test.js's capability-count formula) for the 2 new capabilities",
                      "Full backend suite green: 2950 passed / 177 skipped, zero regressions",
                      "Ran available local hygiene gates: prisma validate, prisma generate, npm run attack:verify, git grep for the CI hygiene job's exact credential-shaped-literal patterns and committed-.env/build-artifact checks against every new file - all clean",
                      "Updated README.md, docs/ai/SECURITY.md, docs/ai/HANDOFF.md, docs/ai/STATE.yaml",
                      "Committed (36a058e), pushed feat/phase-8c-ai-assistance-mvp, and watched CI to completion: run 31169482260, ALL jobs green, including the two gates that could not run locally - 'migrate deploy from an empty database' + 'No drift between schema and applied migrations' (Prisma schema and migration history job) and eval:phase5/eval:phase7 (Core evaluators job) - plus Backend tests against real PostgreSQL, Frontend lint/tests/build, Secrets and generated artifacts, and the Browser suite (Chromium). Released the writer lock."
                  ],
    "validation":  {
                       "backend":  "PASS - 2950 passed / 177 skipped locally, zero regressions; also PASS against real PostgreSQL in CI",
                       "prisma_validate":  "PASS",
                       "prisma_generate":  "PASS",
                       "prisma_migrate_from_zero":  "PASS (CI) - run 31169482260, 'Prisma schema and migration history' job: 'migrate deploy from an empty database' and 'No drift between schema and applied migrations' both green. Not run locally (Docker's daemon never came up in this sandbox); CI's own Postgres service container supplied the real proof.",
                       "evaluators":  "PASS (CI) - run 31169482260, 'Core evaluators' job: eval:phase5 (framework mappings, guarded AI assistance) and eval:phase7 (no-key startup, offline operation, AI off) both green, alongside every other phase evaluator. Not run locally for the same DB reason.",
                       "secret_hygiene":  "PASS - no .env file read/printed/committed; no new credential or API key introduced (no new provider); git grep for CI's exact credential-shaped-literal patterns run manually against every new file AND by CI's own 'Secrets and generated artifacts' job, both clean",
                       "no_live_calls_in_tests":  "PASS - every test injects its own runtime/provider or uses the mock provider; aiAssistSafetyBoundaries.test.js explicitly proxies global.fetch and asserts zero calls in a disabled run",
                       "attack_verify":  "PASS",
                       "ci":  "PASS - https://github.com/haiderchattha99-ali/ThreatNeXus/actions/runs/31169482260 (all jobs green: schema/migration, frontend, core evaluators, backend-vs-real-Postgres, secrets scan, Chromium browser suite; 'Mutation and concurrency gates' is a manual-trigger-only job, not required, not run)",
                       "git_status":  "Clean diff, explicit paths staged only; docs/codex/ untouched throughout (locally excluded via .git/info/exclude, not the shared .gitignore); backend/.env and the root .env (if present) never read or staged"
                   },
    "known_issues":  [
                         "F-drive start-task.ps1 throws against this repo's STATE.yaml schema (assumes a current_work field this project's schema does not have). Worked around by writing STATE.yaml/WRITER_LOCK.json directly in the script's intended shape rather than patching shared F-drive tooling for an unrelated repo. Not fixed.",
                         "No frontend surface for Phase 8C - deferred to Phase 8C.1 per this ticket's own explicit fallback clause.",
                         "No live AI provider exists in this repository (same as Phase 5) and therefore no live smoke script - a future ticket adding one needs a DECISIONS.md entry first, per this repo's own convention.",
                         "docs/codex/ remains untracked and untouched, per the one-writer boundary."
                     ],
    "next_action":  "None required to close this ticket - CI is green. Recommended next steps, no particular priority: (1) Phase 8C.1 - the Finding-detail frontend surface for these suggestions, (2) in-app user management (manage:users capability still unused, flagged since Phase 8B), (3) a third live provider, or (4) resolve Shadowserver access/licensing terms.",
    "checkpoint_ref":  "36a058e",
    "updated_at":  "2026-08-07T11:35:00.0000000Z",
    "next_writer":  "unassigned"
}
