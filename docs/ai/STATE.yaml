{
    "schema_version":  1,
    "ticket":  "TNX-P10C1-TRUTHFUL-TERMINAL-STATES",
    "title":  "Phase 10C-1 - truthful terminal states on the Finding enrichment summary read model",
    "status":  "ready_for_pr",
    "owner":  "claude",
    "reviewer":  "backend-logic-reviewer (design gate: READY on contract v4; implementation gate: SAFE TO COMMIT, 0 P0/P1, 1 P2 fixed same session)",
    "branch":  "feat/phase-10c1-truthful-terminal-states",
    "worktree":  "F:\\AI-Worktrees\\ThreatNeXus\\phase-10c1",
    "starting_commit":  "13f7e24",
    "goal":  "GET /api/findings/:id/enrichment/summary collapsed materially different provider truths into the same status/evidenceAvailable look (a positive result vs 'queried, nothing on file'; a rate limit vs a post-contact ambiguity vs a generic failure; a terminally dead-lettered job reading PENDING because its delegate lagged; VULNERABILITY_ENRICHMENT claiming evidence from job completion alone). This ticket splits those apart using only data the service already reads, with zero new write sites.",
    "scope":  {
                  "included":  [
                                   "backend/src/services/enrichmentOrchestration/enrichmentDecisionCodes.js - three new SUMMARY_STATUSES values (NO_RECORD, RATE_LIMITED, AMBIGUOUS), SUMMARY_STATUS_PRECEDENCE (replaces rollUp's inline array, proven complete by a set-equality test), a new sibling EXECUTION_SKIP_REASONS vocabulary (4 codes) kept OUT of SKIP_REASONS/isKnownSkipReason so the run-item serializer's filter is untouched",
                                   "backend/src/services/enrichmentOrchestration/enrichmentSummaryReadService.js - statusForJobState splits SUCCEEDED/NO_RECORD; IOC_STATUS_TO_SUMMARY splits SUCCESS/NOT_FOUND; refineUnavailableJobStatus/summaryForIocDelegate recover RATE_LIMITED/AMBIGUOUS from closed errorCode/terminalReasonCode only; resolveSubjectState now lets a terminal Phase-10 job outrank a still-PENDING delegate and reports source: ORCHESTRATION_JOB when it does; evidenceAvailable forced false whenever source === VULNERABILITY_ENRICHMENT; rollUp uses the new precedence array with a safe (most-unsettled, not statuses[0]) fallback",
                                   "backend/tests/unit/enrichmentSummaryReadService.test.js - expanded from 30 to 32 tests covering every defect/invariant in the contract, including the delegated-IOC rate-limit path added after the implementation review",
                                   "frontend/src/constants/findingEnrichment.js - NO_RECORD/RATE_LIMITED/AMBIGUOUS labels; four new execution-time skipReason labels; AUTOMATIC_BUDGET_ZERO/MANUAL_BUDGET_ZERO relabeled 'set to zero' (was 'exhausted', which is now reserved for the new execution-time code)",
                                   "frontend/src/components/FindingEnrichmentPanel.jsx - the stale-notice gate no longer requires status === 'COMPLETED' (a stale NO_RECORD row now also shows it)",
                                   "docs/ai/PHASE-10C1-TRUTHFUL-TERMINAL-STATES-CONTRACT.md (new) - the canonical, persisted v4 contract",
                                   "docs/ai/PHASE-10A1-API-CONTRACT.md - Section 4's status vocabulary amended per its own approval rule",
                                   "docs/ai/DECISIONS.md - D-P10C1-01 records the decision and the Section 4 amendment"
                               ],
                  "excluded":  [
                                   "Any write site - all five sites that already write terminalReasonCode/errorCode (enrichmentWorker.js x3, enrichmentDirectExecutionService.js, enrichmentTargetedIocService.js) are read-only from this ticket's perspective; nothing was added or changed there",
                                   "SUCCESSFUL_JOB_STATES / FAILED_JOB_STATES / SKIPPED_JOB_STATES / recomputeRunState - byte-identical to base; persisted EnrichmentRun.state semantics unchanged",
                                   "Per-source vulnerability outcomes (VulnerabilityProviderStatus) - out of scope per the contract's P1-1 scope boundary; this service still never reads that table",
                                   "Migration, auth/authz, quota, concurrency, provider adapters, worker execution, Docker/CI wiring - none touched",
                                   "10C-2 (force/justification UI, run polling, credential/budget UI) - not started",
                                   "PR creation, merge to main - deferred; not opened per instruction"
                               ]
              },
    "completed":  [
                      "Design gate (prior session): backend-logic-reviewer found 6 P1 on the original contract draft; all closed in v3; a surgical re-check found 1 remaining P1 (P1-1, VULNERABILITY_ENRICHMENT evidenceAvailable scope) and 1 non-blocking P2 (provenance on terminal-job precedence); both closed in v4; final narrow closure check returned READY",
                      "Verified live state before implementing: origin/main exactly at 13f7e24 (no drift from the approved base), worktree created clean from that exact commit, no .ai-team/WRITER_LOCK.json anywhere (documented pre-existing gap, same as every phase since 8B), primary checkout confirmed untouched (same pre-existing Phase 9 dirty paths as session start)",
                      "Implemented the full backend read-model split plus the two bounded frontend fixes (P2-2 stale-notice gate, P2-3 budget-zero relabel), grounding every change against the actual current source (not just the contract text) - checkpoint commit 9ac0ec7",
                      "Full backend suite against real PostgreSQL (25 migrations from empty, CI-equivalent env vars): 2900 passed in the parallel run, 613 skipped, 7 files timed out under local parallel-file CPU contention (a documented local-only trap: beforeAll bcrypt hashing across files exceeding the 10s hook timeout) - all 7 re-run in isolation and passed clean (374/374), including phase10a1RouteAuthorization.test.js (52/52), the file closest to the changed surface. Net: 3274 passed, 613 skipped, 0 failed",
                      "Real-Postgres round-trip proof (not the fake-client unit test): directly seeded 5 representative terminal states via Prisma (a DEAD_LETTER Phase-10 job with a still-PENDING IocEnrichment delegate on abuseipdb; NO_RECORD on censys; FAILED+PROVIDER_RATE_LIMITED on greynoise; DEAD_LETTER+AMBIGUOUS_AFTER_CONTACT on netlas; SUCCEEDED-but-expired-freshUntil on shodan) and called the real getFindingEnrichmentSummary against real Postgres - every row resolved exactly as the contract requires, including the highest-risk defect-6/provenance case (abuseipdb -> AMBIGUOUS, source=ORCHESTRATION_JOB)",
                      "Real-browser proof (Chromium/Playwright against that same seeded stack): the panel rendered 'Ambiguous - needs manual review' (x2, both the delegated and direct paths), 'Nothing on file', 'Rate limited', and 'Lookup completed' with a visible stale notice - zero console errors. The pre-existing findingEnrichment.spec.js (8 tests, real integration wiring for the reachable default states) also re-ran clean against the real stack",
                      "One bounded read-only implementation review (backend-logic-reviewer) against the actual base(13f7e24)..head(9ac0ec7) diff, the contract, and the verification evidence above: 0 P0/P1, 1 P2 (missing unit coverage for the delegated-IOC RATE_LIMITED mapping - the direct-job path was tested, the IOC-delegate path was only proven via the real-Postgres script). Verdict: SAFE TO COMMIT: YES",
                      "Closed the one P2: added the missing unit test, re-ran the file (31/31 -> 32/32 after a second addition), committed as the smallest possible closure commit (a455f38)",
                      "Hygiene-equivalent local checks: git diff --check clean on the ticket's own diff (13f7e24..HEAD, exit 0), no committed .env, no credential-shaped literal, no generated artifact committed",
                      "Pushed feat/phase-10c1-truthful-terminal-states. CI run 31943085758 at final SHA a455f38: 6/6 required jobs green (hygiene, schema, backend, frontend, evaluators, browser). The browser-suite job's FIRST attempt failed on one unrelated test (attack.spec.js, the ATT&CK navigator - two transient 404 resource-load errors on the very first test of a 63-test run, only the ADMIN role variant); all 8 findingEnrichment.spec.js tests passed clean on that same attempt. Reran the failed job only (gh run rerun --failed) at the identical SHA - green, confirming the flake was unrelated CI cold-start noise, not a regression"
                  ],
    "validation":  {
                       "backend_focused_tests":  "PASS - 32/32, backend/tests/unit/enrichmentSummaryReadService.test.js (plus enrichmentOrchestrationInertness.test.js unchanged and green)",
                       "backend_full_suite_real_pg":  "PASS - 3274 passed / 613 skipped / 0 failed (2900 + 7 files re-run in isolation after documented local parallel-file contention; not a regression, see completed[])",
                       "frontend_focused_tests":  "PASS - 25/25 FindingEnrichmentPanel.test.jsx (23 original + 2 new: NO_RECORD/RATE_LIMITED/AMBIGUOUS labels, P2-2 stale-on-NO_RECORD)",
                       "lint":  "PASS - oxlint clean on all changed frontend files",
                       "build":  "PASS - vite build clean, no secret-shaped literal in the bundle",
                       "real_postgres_proof":  "PASS - direct Prisma seed of 5 representative terminal states + a real call to getFindingEnrichmentSummary against real Postgres; every row matched the contract exactly (see completed[] for the exact mapping)",
                       "real_browser_proof":  "PASS - Chromium/Playwright against the real backend+DB stack; the new statuses render with their correct distinct labels and zero console errors; the pre-existing findingEnrichment.spec.js suite (8/8) unaffected",
                       "implementation_review":  "SAFE TO COMMIT: YES - backend-logic-reviewer, 0 P0/P1, 1 P2 (closed same session, see completed[])",
                       "hygiene_local":  "PASS - git diff --check clean on the ticket diff, no committed .env, no credential-shaped literal, no generated artifact",
                       "primary_checkout":  "VERIFIED CLEAN at every checkpoint - same pre-existing Phase 9 dirty paths as session start, never touched",
                       "ci":  "GREEN - run 31943085758, head SHA a455f3853f24ccb9df9326700efeb557ecb57db9 (exactly git rev-parse HEAD), all 6 required jobs succeeded (Secrets and generated artifacts, Prisma schema and migration history, Backend tests, Frontend lint/tests/build, Browser suite Chromium, Core evaluators) after one job-level rerun of a confirmed-unrelated flake. 'Mutation and concurrency gates' is manual-trigger-only, correctly skipped"
                   },
    "known_issues":  [
                         "Resolves the known_issue recorded in the prior TNX-P10B-ENRICHMENT-VISIBILITY STATE.yaml: 'COMPLETED at the persistent summary layer collapses a genuine positive result and a genuine queried-nothing-on-file result' - this is now split into COMPLETED vs NO_RECORD",
                         "The '.ai-team/WRITER_LOCK.json' mechanism still does not exist in this repository (same pre-existing gap since Phase 8B) - single-writer discipline was manual and honest, not a working lease",
                         "Per-source vulnerability outcomes (VulnerabilityProviderStatus) remain unreachable from this summary endpoint - deliberately out of scope (contract P1-1 scope boundary); a future ticket would need a repository addition plus a three-source roll-up to reach them",
                         "No force/justification UI, run polling, or credential/budget UI - unchanged from Phase 10B, still backlog if an analyst asks for them (10C-2 candidate, not started)"
                     ],
    "next_action":  "READY FOR PR. Tier-2 verification/delivery stage complete: verified (real-Postgres + real-browser proof), reviewed (backend-logic-reviewer, SAFE TO COMMIT), fixed (the one P2), committed (9ac0ec7, a455f38), pushed, CI green (run 31943085758 at a455f38). No PR opened per instruction. Ready for a human to open the PR when desired; do not start 10C-2.",
    "checkpoint_ref":  "a455f38",
    "updated_at":  "2026-08-16T11:15:00.0000000Z",
    "next_writer":  "claude"
}
