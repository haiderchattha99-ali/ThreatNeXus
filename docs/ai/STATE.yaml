{
    "schema_version": 1,
    "ticket": "TNX-FINAL-EVIDENCE-CONSOLIDATION",
    "title": "Consolidate duplicate final-evidence documentation into one canonical source per subject",
    "status": "pr_open",
    "owner": "unassigned",
    "reviewer": "none (bounded docs-only consolidation; no independent review requested)",
    "branch": "docs/evidence-consolidation",
    "worktree": "F:\\AI-Worktrees\\ThreatNeXus\\evidence-consolidation",
    "starting_commit": "fbffbe325e94969d459b690cee4bf46414f970ed",
    "goal": "PR #31 (TNX-DOC-EVIDENCE-PREP) and PR #32 (TNX-FINAL-DEMO-DOC-EVIDENCE) both merged to main and independently produced evidence documents on the same three subjects under different filenames, plus two overlapping screenshot directories. Leave exactly one canonical evidence source per subject with no loss of unique facts, provenance, measurements, limitations, source SHAs, dates or reproducibility metadata. Docs-only: no product code, no dependency, no README rewrite, no Master/Playbook/Handbook drafting, no provider call, no review loop.",
    "current_work": "",
    "files_changed": [
        "docs/evidence/CONTROLLED-LIVE-CANARY-RECORD.md - canonical; folded in the compose port-mapping operational note from the superseded file",
        "docs/evidence/CONTROLLED-LIVE-CANARY.md - superseded stub, points to canonical, full text retained in Git history at 2612cbc",
        "docs/evidence/EXTERNAL-DATA-ACCESS-RECORD.md - canonical; folded in the 'no national exposure statistics' presentation caution as new section 5",
        "docs/evidence/EXTERNAL-DATA-ACCESS.md - superseded stub",
        "docs/evidence/PRODUCTION-SIZING-MEASUREMENTS.md - canonical; M8 rewritten to explicitly distinguish the authoritative CI-backed validation baseline (3417 at ee1146b, 3460 current at 2612cbc) from this session's own local resource-contention observation (3384/273/5 timeout-affected files, non-authoritative); appendix added for the superseded file's genuinely additional same-day measurements (process RSS, single-sample idle spot check, alternate cold-start definition)",
        "docs/evidence/PRODUCTION-SIZING.md - superseded stub",
        "docs/evidence/screenshots/README.md - superseded stub; the directory's two fully-redundant images removed, its two genuinely unique images moved (pixels unaltered) into the canonical library",
        "docs/assets/screenshots/final/demo-readiness-finding-a-decision-first.png (moved, unaltered)",
        "docs/assets/screenshots/final/demo-readiness-enrichment-before-request.png (moved, unaltered)",
        "docs/assets/screenshots/final/README.md - new 'Demo-readiness supporting captures' section for the two moved images",
        "docs/demo/DEMO-READINESS.md - one stale cross-reference corrected (CONTROLLED-LIVE-CANARY.md -> CONTROLLED-LIVE-CANARY-RECORD.md)",
        "docs/ai/STATE.yaml, docs/ai/HANDOFF.md"
    ],
    "root_cause": "PR #31 and PR #32 branched from a common ancestor and were developed concurrently without either author aware of the other's evidence documents, so both independently wrote records covering the same three subjects (controlled live canary, external data access, production sizing) under different filenames, and both independently captured demonstration screenshots into different directories. Neither branch's CI nor the merge process detects semantic duplication of Markdown content by different filenames, so both merged cleanly and the duplication surfaced only on inspection after both were on main.",
    "final_contract": "Exactly one canonical file per subject survives as a live, citable document: the -RECORD/-MEASUREMENTS suffixed files (originally from PR #31), because they had the stronger provenance structure (fact/attestation/status classification, source-commit tables, review-status records) even where the non-suffixed PR #32 files were sometimes more current. Every fact unique to a superseded file was identified by an explicit line-by-line comparison and folded into the canonical file before the duplicate was reduced to a supersession stub carrying a superseded-by pointer, date and source commit. No file was silently deleted; every superseded file's full original text remains recoverable from Git history at its authoring commit. The screenshot library at docs/assets/screenshots/final/ is canonical; the smaller docs/evidence/screenshots/ directory contributed its two genuinely unique captures (pixels unaltered) and was otherwise retired.",
    "scope": {
        "included": [
            "Line-by-line comparison of all three duplicate document pairs, resolving conflicts only from repository evidence (grep-verified source commits, dated test-count provenance) never from preference for length",
            "The M8 backend-validation-baseline conflict named explicitly by the ticket brief: 3417 (ee1146b, pre-PR32) vs 3460 (2612cbc, current, CI-backed) vs 3384/273/5-timeout (this session's own local contention observation) - resolved as three distinct, clearly labelled facts, none overwriting another",
            "Screenshot library comparison by content, not just file count - two of four smaller-library images were fully redundant with the 40-image final library; two proved a fact (the pre-request enrichment state of the specific primary demonstration Finding) the larger library did not capture and were preserved",
            "One stale cross-reference in docs/demo/DEMO-READINESS.md corrected to the canonical filename",
            "Supersession stubs with superseded-by/date/source-commit headers on every retired file, per the brief's explicit requirement"
        ],
        "excluded": [
            "Any product code, frontend behaviour, dependency, or provider-call change",
            "README rewrite",
            "Master/Playbook/Handbook drafting - the next phase",
            "Any independent reviewer or review/audit cycle - explicitly not requested for this bounded closure",
            "Re-running or re-measuring anything - every canonical figure is transcribed from an existing dated source, never re-observed"
        ]
    },
    "completed": [
        "Read all six evidence documents and both screenshot directories in full before making any keep/archive decision",
        "CONTROLLED-LIVE-CANARY-RECORD.md kept as canonical (fuller provenance: 4-entry source-commit table with reproduction commands, reviewer-status record, explicit proves/does-not-prove scope). Its one gap versus the shorter file - the compose port-mapping operational note - was folded in as a new closing section",
        "EXTERNAL-DATA-ACCESS-RECORD.md kept as canonical (fact/attestation/status classification, repository ticket #7ibziiin which the shorter file omitted entirely). Its one gap - the 'not national cyber exposure statistics' presentation caution - was folded in as new section 5",
        "PRODUCTION-SIZING-MEASUREMENTS.md kept as canonical (rigorous three-sample M2, CPU/RAM/disk identity, M4 reporting-discrepancy disclosure, M6 real-denominator ingestion growth test, full A/B/C/D sizing tiers with TLS/retention/HA marked proposed). M8 rewritten to explicitly resolve the exact three-way validation-count conflict the brief named, rather than letting the contention run stand as the only backend-suite figure. An appendix preserves the shorter session's genuinely additional same-day data points (process RSS, spot idle sample, alternate cold-start definition) without duplicating the full protocol",
        "Screenshots: verified by reading both README narratives, not by filename, that 2 of 4 images in the smaller library were fully redundant and 2 proved unique, necessary evidence (the exact pre-request state of the specific primary demonstration Finding used by docs/demo/DEMO-READINESS.md, as opposed to the unrelated Finding shown in the larger library's decision-first captures). Moved those two with git mv (no re-encoding, pixels unaltered), removed the two redundant ones, documented the addition in the canonical README",
        "Verified via grep across the full repository which files reference the old filenames before editing anything; found and fixed the one live stale cross-reference (docs/demo/DEMO-READINESS.md); confirmed no other product, test, or evaluator file references any of the retired paths",
        "git diff --check clean; confirmed zero backend/frontend/package.json/lockfile changes in the diff; confirmed zero provider calls made (docs-only task, no stack was started)"
    ],
    "validation": {
        "passing": [
            "git diff --check clean",
            "git diff --stat confirms zero files under backend/src, backend/tests, frontend/, package.json or any lockfile",
            "grep across HEAD for the three retired filenames and docs/evidence/screenshots/ found exactly one live stale reference (docs/demo/DEMO-READINESS.md line 227), which was fixed; the canonical -RECORD/-MEASUREMENTS filenames were already the ones referenced from docs/assets/screenshots/final/README.md and the canonical external-data file",
            "Exactly one canonical .md source per subject remains citable (three canonical + three supersession stubs, all six files present, none deleted)",
            "Screenshot index in docs/assets/screenshots/final/README.md resolves to real files: verified every new table row's filename exists on disk after the git mv",
            "Every superseded stub carries superseded-by, date and source commit, per the brief"
        ],
        "failing": []
    },
    "known_issues": [
        "The two sizing sessions were taken on the same host on the same day but four commits apart (2bda0e5 vs 2612cbc); their M2/M3/M7-equivalent figures differ slightly because they measure genuinely different things (three samples vs one, container memory vs process RSS, health-probe-ready vs first-200), not because either is wrong. This is disclosed in the canonical file's appendix rather than resolved by discarding one.",
        "The four-commit gap between the two sizing sessions means the canonical file's M1-M7,M9,M10 protocol was measured at 2bda0e5, one commit before the demo:reset/demo:preflight tooling existed; this is unlikely to materially change idle/startup/bundle figures (that tooling is operator-invoked, not part of the running stack) but was not re-measured, per the brief's instruction not to re-run anything.",
        "docs/ai/HANDOFF.md and docs/ai/STATE.yaml are rolling single-ticket files per this project's established convention; this commit overwrites the TNX-FINAL-DEMO-DOC-EVIDENCE ticket's entries. That ticket's outcome (PR #32 merged, fbffbe3) is preserved in Git history and in this file's own root_cause/scope narrative, not lost."
    ],
    "next_action": "Await CI on docs/evidence-consolidation and PR review. This closes engineering, security, frontend and demo-readiness work, and evidence documentation is now consolidated to one canonical source per subject. Next phase: MASTER OFFICIAL SYSTEM AND HANDOVER DOCUMENT.",
    "checkpoint_ref": "68170e21dc9bc5dd627f205202bc110e34f9efd1",
    "updated_at": "2026-08-19T18:30:00.0000000Z",
    "next_writer": "unassigned"
}