{
    "schema_version":  1,
    "ticket":  "TNX-P10B-ENRICHMENT-VISIBILITY",
    "title":  "Phase 10B - analyst-facing enrichment visibility UI (read model + trigger)",
    "status":  "ready_for_pr",
    "owner":  "claude",
    "reviewer":  "uiux-pro-reviewer (P1 heading hierarchy + P2 misleading lookup-state copy, both fixed; see validation.uiux_review)",
    "branch":  "feat/phase-10b-enrichment-visibility",
    "worktree":  "F:\\AI-Worktrees\\ThreatNeXus\\phase-10b",
    "starting_commit":  "78f2a69",
    "goal":  "Expose the already-merged Phase 10 enrichment orchestration read model (docs/ai/PHASE-10A1-API-CONTRACT.md) to analysts on the Finding detail screen, truthfully distinguishing NO_SUBJECT / NOT_REQUESTED / PENDING / COMPLETED (fresh vs stale) / UNAVAILABLE / SKIPPED (with reason) for all six known providers, plus the existing analyst trigger action (POST .../enrichment/runs) with durable contacted-state feedback. No backend route added; no Phase 10A-2 execution semantics touched.",
    "scope":  {
                  "included":  [
                                   "frontend/src/components/FindingEnrichmentPanel.jsx (new) - the panel: provider-by-provider summary table (GET .../enrichment/summary), capability-gated 'Request enrichment' action (POST .../enrichment/runs), and a 'last request' item table with a manual 'Check status' refresh (GET .../enrichment/runs/:runId) that renders `contacted` directly from the stored backend field",
                                   "frontend/src/constants/findingEnrichment.js (new) - closed-vocabulary label dictionaries mirroring backend/src/services/enrichmentOrchestration/enrichmentDecisionCodes.js and enrichmentSubject.js (SUMMARY_STATUSES, item DECISIONS, SKIP_REASONS, SUMMARY_SOURCES, provider purposes/labels, executionState copy, run outcome copy)",
                                   "frontend/src/services/api.js - additive findingEnrichmentOrchestrationService (getSummary/createRun/getRun) reusing the three existing Phase 10A-1 routes; reportIngestionService and every other export unchanged",
                                   "frontend/src/pages/FindingDetail.jsx - mounts <FindingEnrichmentPanel> as its own panel, separate from the pre-existing legacy 'IP reputation context' panel (which reads the older single-provider AbuseIPDB cache row and is unchanged)",
                                   "frontend/src/components/FindingEnrichmentPanel.test.jsx (new) - 18 focused tests: all six provider states render distinctly, a stale COMPLETED answer keeps its StaleNotice, NVD verified-CVE subjects each get their own row, executionState is never conflated with 'executed', capability gating (trigger:finding-enrichment) on the request control, and contacted is proven to come from the stored field (Yes/Not yet for an ELIGIBLE item, em dash for a policy-skipped one) across create + refresh"
                               ],
                  "excluded":  [
                                   "Any backend route, controller or service under backend/src/services/enrichmentOrchestration/ - zero-diff",
                                   "Any change to Phase 10A-2 worker/quota/execution semantics",
                                   "Risk v1 (backend/src/services/risk/) - untouched",
                                   "The legacy 'IP reputation context' panel and its single-provider AbuseIPDB cache read - untouched, left as-is, deliberately not merged with the new panel",
                                   "force/justification UI on the trigger action - the backend supports it, but this ticket's request control only issues a plain default run; add when an analyst actually needs to force a stale result",
                                   "Auto-polling or any automatic provider-execution trigger - the panel only reads on mount and on an explicit analyst action (request or 'Check status')",
                                   "PR creation, merge to main, full CI-equivalent gate run - deferred to the next stop boundary per the Tier-2 lifecycle"
                               ]
              },
    "completed":  [
                      "Mandatory start gate: read F:/Ismail-AI-Dev-Team global AGENTS.md/CLAUDE.md, GLOBAL-MEMORY.md, project AGENTS.md/CLAUDE.md, docs/ai/{STATE.yaml,HANDOFF.md} as they stood on origin/main, and the Phase 10A-1/10A-2 binding API contracts before writing anything",
                      "Verified live state: origin/main at 78f2a69 (PR #21, Phase 10A-2 merge) is ahead of local main (3638a39) and far ahead of the primary checkout's branch (5fe93d2, based on c3f8a4b, predates even the Phase 9C ingestion-repair PR) - confirmed no Phase 10B backlog/contract exists anywhere in canonical docs (grepped origin/main tree and the phase-10-planning worktree for '10B'), so scope was derived directly from the binding PHASE-10A1-API-CONTRACT.md read model plus the current FindingDetail.jsx/api.js surface, not invented",
                      "MISTAKE MADE AND CORRECTED: initial edits were made directly in the primary checkout's working tree (violates the explicit 'do not touch the primary checkout' instruction). Caught via git status before anything was committed. Fully reverted with `git checkout HEAD -- <2 tracked files>` plus deleting the 3 untracked new files, verified with git -C status showing the primary checkout back to its exact session-start state (only the pre-existing Phase 9 dirty paths remain). All work re-homed into this worktree",
                      "A second mistake surfaced during that recovery: a `cd <worktree> && git checkout origin/main -- frontend/src/services/api.js` command actually executed against the PRIMARY checkout (confirmed via `pwd`), staging an unintended index change there - this matches the documented [[threatnexus_worktree_cd_hazard]] memory. Also fixed by the same full revert above. From that point on, every shell command paired `cd <dir> && pwd && <command>` in ONE call, or used `git -C <dir>`, and pwd output was checked before trusting any result",
                      "Created worktree F:\\AI-Worktrees\\ThreatNeXus\\phase-10b on branch feat/phase-10b-enrichment-visibility from origin/main (78f2a69); no writer-lock script was available for this repo (documented pre-existing gap, same as every phase since 8B), so proceeded under manual single-writer discipline after confirming no .ai-team/WRITER_LOCK.json exists",
                      "Implemented FindingEnrichmentPanel.jsx, findingEnrichment.js label dictionaries, the additive api.js service, and the FindingDetail.jsx mount point",
                      "npm ci in the worktree (fresh node_modules, not shared with the primary checkout)",
                      "18 focused component tests written and passing; the 16 pre-existing FindingAiAssistPanel tests (also rendered on FindingDetail) re-run clean alongside them (34/34 total)",
                      "oxlint clean on all 5 changed/added files; `vite build` production build clean",
                      "TIER-2 VERIFICATION STAGE: independently re-derived and confirmed the server-side authorization proof for POST /enrichment/runs by reading backend/src/routes/enrichmentOrchestrationRoutes.js + backend/src/lib/roles.js (TRIGGER_FINDING_ENRICHMENT: ADMIN+ANALYST only) and actually RUNNING backend/tests/integration/phase10a1RouteAuthorization.test.js (52/52 passing, real HTTP against the mounted app) rather than trusting the DECISIONS.md draft's claim - confirmed ADMIN/ANALYST 202, REVIEWER/VIEWER 403 with nothing recorded, unauthenticated 401, force=true grants no bypass. Frontend gate (CAPABILITIES.TRIGGER_FINDING_ENRICHMENT = 'trigger:finding-enrichment' in frontend/src/constants/capabilities.js) matches the backend capability string exactly - no new authorization surface, Tier-2 stands",
                      "Dispatched one read-only uiux-pro-reviewer pass against the exact 9 questions in the ticket brief. Found 1 P1 (heading hierarchy: Panel used titleLevel='h3' in all 3 FindingEnrichmentPanel render paths while every sibling FindingDetail panel uses the default h2, breaking screen-reader section navigation) and 1 P2 (the 'Lookup state' column showed 'Not yet queued' for permanently policy-skipped items, implying eventual queuing that will never happen). Both fixed: dropped titleLevel='h3' at all 3 call sites; 'Lookup state' cell now shows '-' for any item.decision !== 'ELIGIBLE', matching the existing Contacted column's convention. P3/backlog items (COMPLETED collapsing SUCCEEDED/NO_RECORD at the persistent summary layer, execution-time SKIPPED carrying no reason) are inherited Phase-10A backend read-model decisions, correctly and honestly surfaced by this panel, not defects this ticket introduced or can fix without a backend contract change",
                      "Re-ran FindingEnrichmentPanel.test.jsx (19/19) and FindingAiAssistPanel.test.jsx (16/16) after the two fixes - no regressions. Re-ran oxlint (clean) and vite build (clean) on the final diff",
                      "Ran the existing frontend/e2e/findingEnrichment.spec.js Chromium suite against the REAL stack (docker compose postgres+backend, seed:users + seed:demo, vite build + playwright preview webServer): all 8 tests pass - panel renders all six providers, execution-paused state shown truthfully, VIEWER sees no Request enrichment button, ANALYST can request and see a recorded outcome, no horizontal overflow at tablet/mobile, zero console errors for all four roles. One environment-setup false failure hit and corrected along the way (not a code defect): Playwright's default baseURL is http://127.0.0.1:4173, not http://localhost:4173 - the manually-started backend's CORS_ORIGIN had been set to the localhost variant, so every browser fetch silently failed with status -1 (confirmed via the Playwright trace's 0-trace.network file) while curl to the same endpoint succeeded. Fixed by matching CORS_ORIGIN to the actual origin; recorded as [[ENGINEERING-LESSONS]] in F:/Ismail-AI-Dev-Team/memory so it is not rediscovered next time",
                      "Froze the Phase 10B V1 scope in docs/ai/DECISIONS.md (D-P10B-01) - already drafted correctly by the prior session, left as-is",
                      "Recorded the writer-lease tooling gap ('.ai-team/WRITER_LOCK.json' has never existed in this repo, same as every phase since 8B) as a standing cross-project backlog item in F:/Ismail-AI-Dev-Team/memory/ENGINEERING-LESSONS.md rather than re-documenting it silently again this session"
                  ],
    "validation":  {
                       "frontend_focused_tests":  "PASS - 35/35 (19 FindingEnrichmentPanel [18 original + 1 more surfaced by the fix commit] + 16 pre-existing FindingAiAssistPanel), re-run AFTER the two bounded UI/UX fixes, from F:/AI-Worktrees/ThreatNeXus/phase-10b/frontend",
                       "backend_auth_test":  "PASS - 52/52, backend/tests/integration/phase10a1RouteAuthorization.test.js, run from F:/AI-Worktrees/ThreatNeXus/phase-10b/backend with --hookTimeout=30000 (first run hit the known cold-import >10s hookTimeout trap; not a code failure)",
                       "chromium_e2e":  "PASS - 8/8, frontend/e2e/findingEnrichment.spec.js, against a real dockerized postgres+backend (seeded demo data, 11 findings) and the production preview build on 127.0.0.1:4173",
                       "uiux_review":  "COMPLETE - uiux-pro-reviewer, read-only, 1 P1 + 1 P2 found and fixed, P3 items backlogged as inherited backend-contract decisions (see completed[] above for detail)",
                       "lint":  "PASS - oxlint clean on FindingEnrichmentPanel.jsx, FindingEnrichmentPanel.test.jsx, findingEnrichment.js, api.js, FindingDetail.jsx (re-run after fixes)",
                       "build":  "PASS - vite build clean, no new warnings (re-run after fixes)",
                       "api_diff":  "Additive only - `git diff --stat` on frontend/src/services/api.js shows 10 insertions, 0 deletions; reportIngestionService and every pre-existing export verified byte-unchanged",
                       "backend_diff":  "NONE - no file under backend/ touched (verified again this stage; only the 2 bounded frontend fixes were made)",
                       "risk_diff":  "NONE - backend/src/services/risk/ not touched",
                       "primary_checkout":  "VERIFIED CLEAN AGAIN at the start of this stage - C:\\Users\\LENOVO\\Desktop\\ThreatNeXus git status matches its exact session-start snapshot (only the pre-existing Phase 9 dirty paths), confirmed before any worktree command ran",
                       "full_ci_equivalent_gate":  "Proportional Tier-2 gate complete: frontend suite, lint, build, backend auth test, Chromium E2E all green. Full backend suite / evaluators / Docker demo rehearsal NOT re-run - out of the diff's blast radius (zero backend/risk files touched) per the ticket's own validation-strategy instruction; GitHub Actions CI (which does run the full backend suite) is the closing gate, to be confirmed green after push"
                   },
    "known_issues":  [
                         "force/justification on the trigger action is intentionally not exposed - an analyst who needs to force-refresh a stale result currently has no UI for it (the backend supports it). Backlog item, not required by any binding contract read for this ticket",
                         "The 'Check status' refresh is a manual, single-shot GET - there is no live/auto-polling of a run's progress. Deliberate (Phase 10B scope explicitly excludes autonomous behavior); an analyst must click again to see a job move from PENDING to a terminal state",
                         "This worktree's frontend/node_modules is a fresh npm ci, not shared with the primary checkout or any other worktree - normal for a new worktree, noted so a future session does not assume it is missing by mistake",
                         "The '.ai-team/WRITER_LOCK.json' mechanism does not exist in this repository (recorded as a standing cross-project backlog item in F:/Ismail-AI-Dev-Team/memory/ENGINEERING-LESSONS.md rather than being rebuilt inside this ticket) - single-writer discipline was manual and honest, not a working lease",
                         "COMPLETED at the persistent summary layer collapses a genuine positive result and a genuine 'queried, nothing on file' result into one look - an inherited Phase-10A backend read-model decision (enrichmentSummaryReadService.js), not something this frontend ticket can fix without a backend contract change. The current session's 'last run' table does distinguish them via the raw lookupState field"
                     ],
    "next_action":  "Tier-2 second stop boundary reached (verify -> review -> deliver -> STOP). Stage only the ticket-owned paths (frontend/src/components/FindingEnrichmentPanel.jsx, FindingEnrichmentPanel.test.jsx, frontend/src/constants/findingEnrichment.js, frontend/src/services/api.js, frontend/src/pages/FindingDetail.jsx, frontend/e2e/findingEnrichment.spec.js, docs/ai/*), commit, push feat/phase-10b-enrichment-visibility, confirm CI green, report PR readiness. Do not open a PR. Do not start Phase 10C.",
    "checkpoint_ref":  "pending commit",
    "updated_at":  "2026-08-15T00:00:00.0000000Z",
    "next_writer":  "claude"
}
