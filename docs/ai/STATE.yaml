{
    "schema_version":  1,
    "ticket":  "TNX-P8C1-AI-ASSISTANCE-FRONTEND",
    "title":  "Phase 8C.1 - Frontend AI assistance surface (Finding detail: draft generation, accept/reject, disabled/denied states)",
    "status":  "complete",
    "owner":  "unassigned",
    "reviewer":  "codex",
    "branch":  "feat/phase-8c1-ai-assistance-frontend",
    "worktree":  "C:\\Users\\LENOVO\\Desktop\\ThreatNeXus",
    "starting_commit":  "99c9743",
    "goal":  "Make the Phase 8C backend AI finding-assistance MVP visible and usable in the frontend: a compact analyst-focused panel on Finding detail showing availability state, generate summary/explanation actions (role-gated), the suggestion list with status/timestamps/evidence references, accept/reject actions (role-gated, backend-enforced), and distinct disabled/not-configured/unavailable/denied/error states. No live AI provider. AI remains suggestion-only, never decision-making.",
    "scope":  {
                  "included":  [
                                   "frontend/src/services/api.js: aiFindingAssistService (request/list/accept/reject), reusing the existing aiMappingService.getConfig() for availability - one AI_ENABLED/AI_PROVIDER switch, one config call, shared by both AI surfaces",
                                   "frontend/src/constants/findingAiAssistance.js: presentation vocabulary mirroring backend/src/services/aiAssist/aiAssistRules.js verbatim (suggestion types, StatusBadge status dictionary, reason-code sentences, evidence-reference labels, describeAiAssistError)",
                                   "frontend/src/constants/capabilities.js: READ_AI_FINDING_SUGGESTIONS + REQUEST_AI_FINDING_SUGGESTIONS added, mirroring backend/src/lib/roles.js exactly; decide reuses the pre-existing REVIEW_AI_SUGGESTIONS constant already present",
                                   "frontend/src/components/FindingAiAssistPanel.jsx: the panel itself, built from the Phase 6 design-system idiom (Panel/StatusBadge/States/AVAILABILITY) FindingDetail.jsx already uses, not the older ad-hoc styling FrameworkMappingPanel.jsx (Phase 5) uses - anti-automation-bias rules mirrored from FrameworkMappingPanel (no accept-all, no pre-selected decision, reject reason required, decided drafts stay visible, EXPIRED shown with its staleness reason)",
                                   "Mounted on frontend/src/pages/FindingDetail.jsx as its own full-width Panel section",
                                   "16 new component tests (FindingAiAssistPanel.test.jsx): capability gating x4, disabled/unavailable states x3, generate success/403/raw-error-never-shown x3, decide (reject-reason-required, accept) x2, status rendering x4",
                                   "frontend/e2e/findingAiAssistance.spec.js: 4 fixed scenarios + one per-role loop (7 total), covering disabled-by-default rendering, VIEWER denial, REVIEWER read-only, responsive layout, and zero console errors per role - the populated-draft rendering is deliberately NOT covered here (see known_issues)",
                                   "Docs: README.md (frontend cross-reference on the existing AI finding assistance table row), docs/ai/SECURITY.md (new 'Frontend AI-assistance surface' subsection), docs/ai/HANDOFF.md, docs/ai/STATE.yaml"
                               ],
                  "excluded":  [
                                   "Any live AI provider or OpenAI/Anthropic/Gemini integration",
                                   "Automatic notification sending, automatic finding closure, automatic ATT&CK approval, autonomous agent workflows, background scheduled AI jobs",
                                   "Shadowserver, Shodan, Netlas, GreyNoise, VirusTotal, OTX, MISP integration",
                                   "New UI framework or design-system migration, unrelated dashboard redesign, new animation/motion",
                                   "Any change to the completed Phase 8C backend - zero backend files touched, zero schema changes; this ticket is frontend-only",
                                   "PR creation or merge into main"
                               ]
              },
    "completed":  [
                      "Verified the mandatory start gate: fetched origin, confirmed Phase 8C (feat/phase-8c-ai-assistance-mvp) was already merged into origin/main via PR #12 (99c9743) while local main was 3 commits stale; fast-forwarded local main, branched feat/phase-8c1-ai-assistance-frontend from the updated tip",
                      "Acquired the AI-team writer lock for TNX-P8C1-AI-ASSISTANCE-FRONTEND directly (same F-drive start-task.ps1 schema-mismatch workaround as Phase 8C)",
                      "Read FrameworkMappingPanel.jsx (Phase 5's AI mapping frontend) in full before writing anything, as the interaction-pattern template (capability gating, request/decide flow, anti-automation-bias rules), and FindingDetail.jsx/States.jsx/StatusBadge.jsx/tokens.js as the visual-idiom template (this panel mounts on FindingDetail, which uses the newer Phase 6 design system, not Phase 5's older ad-hoc styling)",
                      "Discovered theme/tokens.js's AVAILABILITY dictionary already distinguishes DISABLED/UNAVAILABLE/RESTRICTED/STALE - reused directly rather than inventing a bespoke status vocabulary for this one panel",
                      "Implemented the full frontend surface: capabilities, API client, constants, panel component, FindingDetail mounting",
                      "Wrote 16 component tests; all passed on the first real run against the actual component (no implementation bugs found this time, unlike Phase 8C's backend where 2 tests needed correcting)",
                      "Ran frontend lint (oxlint) - clean, zero new warnings",
                      "Ran the full frontend test suite - 159 passed (0 failed), up from the 143 baseline by exactly the 16 new tests, zero regressions",
                      "Ran the production build - succeeded; scanned dist/ for the same secret-shaped-literal patterns CI's own gate checks (ABUSEIPDB_API_KEY|NVD_API_KEY|-----BEGIN) - clean",
                      "Docker's daemon (unavailable throughout Phase 8C) came up mid-session, surfacing a STALE docker-compose stack from an earlier session (postgres/backend/frontend containers, images pre-dating the Phase 8C merge). Used it for genuine additional verification without disturbing it: ran `prisma migrate deploy` against the real Postgres (applied the one pending Phase 8C migration, additive, already proven safe by CI), reseeded the four well-known demo accounts with a locally-chosen password via seedUsers.js (idempotent upsert), and ran MY OWN backend process (not the stale container) on a free port against that same database",
                      "Live-verified in a real browser against that real stack: ANALYST/REVIEWER/VIEWER login, the AI assistance panel renders correctly on a real seeded Finding, VIEWER gets the DeniedState with the real capability name from the real server response, REVIEWER sees the panel with no generate control (real capability data, not mocked), the disabled state renders accurately (AI_ENABLED unset = false, the shipped default), zero console errors on any role, no horizontal overflow at mobile (375px) or tablet (768px) widths, and the rest of the Finding-detail page (triage, risk explanation, ownership, timeline) is unaffected by mounting the new panel",
                      "Cleaned up every verification-only artifact: temporary frontend/.env.local and .claude/launch.json deleted, local verification backend process killed, browser preview server stopped - working tree confirmed clean before staging",
                      "Updated README.md, docs/ai/SECURITY.md, docs/ai/HANDOFF.md, docs/ai/STATE.yaml",
                      "Committed (8c49779), pushed, watched CI (run 31173166431): 5 jobs green, 'Browser suite (Chromium)' FAILED on findingAiAssistance.spec.js's VIEWER test - DeniedState's `capability` prop only renders inside its own default fallback text, and passing custom `children` (as the panel's !canRead branch did) suppressed it entirely, so the capability code never appeared for the spec to find. A real bug the new Playwright spec caught on its first real execution, exactly what it is for. Fixed by dropping the custom children and using DeniedState's own fallback (which already names the capability) - re-ran the 16 component tests and lint locally, both still clean",
                      "Committed the fix (5a2aed4), pushed, watched CI again (run 31173470851): ALL jobs green including 'Browser suite (Chromium)' - Frontend lint/tests/build, Secrets and generated artifacts, Prisma schema and migration history, Core evaluators, Backend tests against real PostgreSQL, and the Browser suite. Released the writer lock."
                  ],
    "validation":  {
                       "frontend_lint":  "PASS - oxlint clean, zero new warnings in any new or touched file",
                       "frontend_test":  "PASS - 159 passed / 0 failed (up from 143 baseline by the 16 new tests), zero regressions",
                       "frontend_build":  "PASS - vite build succeeded, production bundle scanned for secret-shaped literals, clean",
                       "browser_verification":  "PASS - live-verified in a real browser against a real backend and a real seeded PostgreSQL database this session (see completed[] for detail); not merely against mocks",
                       "backend":  "UNCHANGED - zero backend files touched by this ticket; the Phase 8C backend suite (2950/177, CI run 31169482260) remains the authoritative backend proof",
                       "e2e_ci":  "PASS (CI) - run 31173470851, 'Browser suite (Chromium)' job green. Its first real execution (run 31173166431) caught a genuine bug (DeniedState capability text suppressed by custom children); fixed and re-verified green. Not run locally - would have required stopping the pre-existing stale Docker containers occupying the default ports.",
                       "ci":  "PASS - https://github.com/haiderchattha99-ali/ThreatNeXus/actions/runs/31173470851 (all jobs green: frontend lint/tests/build, secrets scan, schema/migration, core evaluators, backend-vs-real-Postgres, Chromium browser suite; 'Mutation and concurrency gates' is manual-trigger-only, not required, not run)",
                       "git_status":  "Clean diff, explicit paths staged only; docs/codex/ untouched throughout; backend/.env and the root .env (if present) never read, printed or staged; the temporary local-verification password change to the four demo accounts on the STALE local Docker Postgres is NOT part of this diff and does not affect CI (CI seeds its own fresh database)"
                   },
    "known_issues":  [
                         "The populated-draft rendering (a real generated DRAFT card with evidence tags and accept/reject) cannot be observed through a live browser session anywhere, by design: aiAssistRuntime.js never resolves the mock provider without a test-only flag no production HTTP path ever passes. This is the SAME boundary Phase 8C's backend documents, not a new gap - covered instead by FindingAiAssistPanel.test.jsx (16 tests, mock-injected at the component level).",
                         "This session locally changed the four demo-account passwords on a STALE, pre-existing local Docker Postgres container (left over from an earlier session) in order to browser-verify live. That container/database is not part of this repo's tracked state and does not affect CI, but the next person using that SPECIFIC local Docker stack will need to re-run seed:users or seed:demo with their own password.",
                         "F-drive start-task.ps1 throws against this repo's STATE.yaml schema (assumes a current_work field this project's schema does not have). Worked around, not fixed - same known gap as Phase 8C.",
                         "docs/codex/ remains untracked and untouched, per the one-writer boundary."
                     ],
    "next_action":  "None required to close this ticket - CI is green. Recommended next steps, no particular priority: (1) in-app user management (manage:users capability still unused, flagged since Phase 8B), (2) GreyNoise, Shodan, or Netlas as a fourth live provider, or (3) resolve Shadowserver access/licensing terms.",
    "checkpoint_ref":  "5a2aed4",
    "updated_at":  "2026-08-07T11:55:00.0000000Z",
    "next_writer":  "unassigned"
}
