{
    "schema_version": 1,
    "ticket": "TNX-DOC-EVIDENCE-PREP",
    "title": "Final documentation evidence base - canary rescue, external-data access record, bounded production-sizing measurements, final screenshot library, canonical state reconciliation",
    "status": "pr_open",
    "owner": "claude",
    "reviewer": "unassigned",
    "branch": "docs/final-evidence-prep",
    "worktree": "F:\\AI-Worktrees\\ThreatNeXus\\final-doc-evidence",
    "starting_commit": "2bda0e551bb28af68975e198e21927baba12628e",
    "goal": "Close the factual and evidentiary blockers standing between the merged product and the final official documentation set, WITHOUT drafting any final document, deleting any existing document, performing another review, contacting any provider, or changing any product behaviour or dependency. Evidence only.",
    "current_work": "",
    "program_status": {
        "core_engineering": "closed",
        "security_pass": "closed - PR #29 merged at 702afee (bounded final security assessment, 3 findings fixed)",
        "frontend_polish": "closed / merged - PR #30 merged at 2bda0e5; the one approved R1-R5 pass, no second cycle",
        "documentation_blueprint": "approved - Master (18pp target / 20pp hard max), Analyst & Operations Playbook, Knowledge & Defence Handbook, README last",
        "documentation_evidence_preparation": "completed - this ticket",
        "next_stage": "MASTER OFFICIAL DOCUMENT DRAFTING"
    },
    "files_changed": [
        "docs/evidence/CONTROLLED-LIVE-CANARY-RECORD.md (new)",
        "docs/evidence/EXTERNAL-DATA-ACCESS-RECORD.md (new)",
        "docs/evidence/PRODUCTION-SIZING-MEASUREMENTS.md (new)",
        "docs/assets/screenshots/final/README.md (new)",
        "docs/assets/screenshots/final/*.png (new - 41 capture masters)",
        "docs/ai/STATE.yaml",
        "docs/ai/HANDOFF.md"
    ],
    "scope": {
        "included": [
            "Rescue of the TNX-P10C4 controlled live GreyNoise canary evidence out of rolling working-state files (HANDOFF.md and STATE.yaml at 914d582) into a permanent, current, non-secret evidence record. No provider was contacted to produce it.",
            "A permanent external-data access record separating REPOSITORY-DOCUMENTED FACT from PROJECT-TEAM ATTESTATION from CURRENT STATUS, covering Shadowserver and Rapid7 / Project Sonar, and preserving the scanner-source versus exposed-host category rule.",
            "One bounded M1-M10 production-sizing measurement session against a disposable local stack with providers, worker, automatic enrichment and AI all off, classifying every figure as MEASURED / REASONED RECOMMENDATION / NOT LOAD-TESTED / NOT MEASURED.",
            "A final screenshot library captured from the production build at this exact commit against a from-zero database with the deterministic demo seed: both Master screenshots, all 18 Playbook captures and 20 supporting captures, with an index recording route, role, state, viewport, date and source commit.",
            "Canonical state reconciliation: STATE.yaml and HANDOFF.md now record PR #30 as merged and documentation evidence preparation as the completed current stage."
        ],
        "excluded": [
            "Any final document. The Master Official Document, the Analyst & Operations Playbook, the Knowledge & Defence Handbook and the README rewrite are all deliberately NOT started.",
            "Any deletion or archival of existing documentation.",
            "Any further product, security or frontend review.",
            "Every backend and frontend source file - git status --porcelain over backend/, frontend/src and frontend/e2e is empty, and no dependency was added or changed anywhere.",
            "Any live provider request. Zero outbound provider contact occurred at any point in this session.",
            "Any concurrency or load test. None was authorised and none was performed; no concurrency or scale capacity figure is claimed.",
            "The primary checkout at C:\\Users\\LENOVO\\Desktop\\ThreatNeXus - never written to. All work was done in an isolated worktree."
        ]
    },
    "completed": [
        "Resolved the live base from Git rather than from a stated SHA: origin/main fetched and confirmed at 2bda0e551bb28af68975e198e21927baba12628e, the same tip the documentation audit used. Created an isolated worktree on docs/final-evidence-prep from that exact commit and acquired writer lease d3de04ef-2322-42f5-9d38-48752443e3aa.",
        "RESCUED the 10C-4 canary evidence. Confirmed 914d582 IS an ancestor of main, but that both files carrying the evidence are rolling working-state documents since overwritten, so the evidence survived only in historical Git state. Transcribed it into docs/evidence/CONTROLLED-LIVE-CANARY-RECORD.md with every required fact preserved and the source commits cited so any reader can verify it with git show. No provider was contacted and no credential value appears anywhere.",
        "Recorded the canary scope limitation explicitly: it proves the approved GreyNoise direct-worker path only, and confers no live proof on abuseipdb, on the delegated lane, on the four legacy synchronous provider routes that contact providers off-ledger, or on any non-local environment.",
        "Wrote docs/evidence/EXTERNAL-DATA-ACCESS-RECORD.md with three never-merged classes. Verified every repository claim in it against source before writing: STATUS.md carries Shadowserver ticket #7ibziiin; PROJECT_PLAYBOOK.md and PROVIDER_GUIDE.md record live scheduled ingestion as out of scope; data/synthetic/README.md states accessible-rdp.synthetic.v1 is NOT an official Shadowserver schema; README.md frames a real report as requested-not-received; and every address in data/synthetic and data/demo falls inside 192.0.2.0/24, 198.51.100.0/24, 203.0.113.0/24 or 198.18.0.0/15.",
        "Recorded the project-team attestation in neutral professional language: Shadowserver responded to the inquiry; supervisor Ms. Ayesha supported the coordination process; the related email/access request did not receive the required approval from AD Threat Intelligence Mrs. Aasia Bibi; Shadowserver data and API access were therefore not treated as available project inputs. Rapid7 Open Data Ticket #140718 was submitted 2026-08-11 with a follow-up sent subsequently, and as of 2026-08-19 no dataset, access or report has been received. Both are recorded as external-data/access status, never as software limitations, and no claim is made that any exposure dataset was obtained.",
        "Executed the M1-M10 measurement protocol against a disposable stack on isolated ports (postgres 15432, backend 15000, frontend 15173) under its own compose project, using a DEDICATED compose file rather than an -f overlay so the ports list replaced instead of concatenating - the operational trap 10C-4 recorded. The separate tnxdemo stack already running on the default ports was left untouched.",
        "M6 was measured twice and both results are reported. The committed historical fixtures produced HISTORICAL outcomes, created no new Findings, and moved pg_database_size by exactly zero bytes three times out of four - direct evidence that a per-finding byte constant would be fiction. A deterministic 500-new-indicator report was then ingested three times through the real POST /api/reports/upload to obtain a real denominator, confirming deduplication (1,500 rows produced 500 Findings) and that a persisted observation costs roughly a quarter of what a new Finding costs.",
        "Captured the final screenshot library from the production build at this commit against a database created from zero and seeded deterministically: 3 master captures, 18 playbook captures and 20 supporting captures, at 1440x900 with device pixel ratio 2, dark scheme, animations frozen, clean Chromium profile, and zero console errors across every capture.",
        "Created exactly ONE state through the real API because the demo seed does not leave it behind: a PENDING closure request on case 1 (ACCEPTED_RISK), needed for the P-10 and P-11 captures. It is disclosed in the screenshot index, its own justification text says why it exists, and no screenshot was altered after capture.",
        "Recorded honestly that a literal RESTRICTED or UNAVAILABLE analyst-dashboard KPI tile is NOT AVAILABLE FROM CURRENT DEMO DATA, because at demonstration scale every section resolves to a real value and forcing one would stage a state the running system is not in. The reproducible truthful distinctions were captured instead: the four risk-factor applicability states, the capability-restricted VIEWER section, and the unified route-level refusal.",
        "Verified the current fact counts the final documents must use: 25 migrations, 165 backend test files, 17 frontend test files, 11 Playwright specs, and a local backend suite result of 3,384 passed / 273 skipped at this commit."
    ],
    "validation": {
        "passing": [
            "Base verified: worktree created at 2bda0e5 on docs/final-evidence-prep, writer lease acquired, primary checkout never written to",
            "M1-M10 executed; every figure classified MEASURED / REASONED RECOMMENDATION / NOT LOAD-TESTED / NOT MEASURED",
            "Screenshot capture: 41 masters, zero console errors and zero page exceptions across every capture",
            "Every repository claim in the external-data record verified against the cited file before being written",
            "Canary record verified against git show 914d582:docs/ai/HANDOFF.md and git show 914d582:docs/ai/STATE.yaml",
            "git diff --check clean"
        ],
        "failing": [],
        "product_code_untouched": "PASS - git status --porcelain over backend/, frontend/src, frontend/e2e and every package.json is empty. No source, dependency, migration, schema or configuration file was changed.",
        "no_live_provider_calls": "PASS - every provider key was empty in the measurement stack, IOC_ENRICHMENT_PROVIDER=mock, AUTO_ENRICHMENT_ENABLED=false, ENRICHMENT_WORKER_ENABLED=false, every budget 0, AI_ENABLED=false. The canary record was transcribed from Git history, not re-run.",
        "no_secrets": "PASS - no credential value is recorded in any new file. The measurement stack used a per-run generated JWT_SECRET and a throwaway local database password, both scratchpad-only and never committed. Screenshots were inspected for credentials, tokens and personal data.",
        "no_load_test": "PASS - no concurrency or load test was performed, and no concurrent-analyst, requests-per-second, throughput or HA capacity figure is claimed anywhere.",
        "disposable_stack_destroyed": "PASS - the measurement stack and its volume were removed with docker compose down -v.",
        "ci": "pending - recorded on push"
    },
    "known_issues": [
        "M8 peak memory is NOT MEASURED. The host process sampler failed on a path-conversion error and the run was not repeated; container-side samples from the same window could not be cleanly attributed to the suite and were discarded rather than reported. Suite duration and pass/skip counts were captured.",
        "Five backend test files failed on 10-second hook timeouts during the local M8 run. This is the documented local contention pattern on this machine - the failures are beforeAll timeouts, they differ between runs, and CI is the authoritative signal. Recorded rather than omitted.",
        "The measurement host was not otherwise idle: unrelated containers from other projects ran throughout. Per-container CPU and memory readings are unaffected, but every elapsed-time figure (M6, M7, M8, M10) was taken under contention and should be read as an upper-ish bound.",
        "Docker reports two different sizes for the same image on this host (docker images SIZE versus docker ps -s virtual size). Both are recorded with their method rather than silently picking one; the ps -s column is the applicable one for single-architecture disk planning.",
        "M3 startup-peak sampling has roughly 2-3 second resolution, which is the cost of docker stats --no-stream itself. A peak shorter than that interval would have been missed.",
        "No retention policy, data-expiry job or archival path exists in the system. Findings, occurrences and audit rows accumulate indefinitely. This is a gap a pilot must decide on, not a setting to configure, and it is recorded in the sizing document rather than papered over.",
        "Database restore has never been tested. pg_dump was measured; pg_restore was not.",
        "The four legacy synchronous provider routes remain an off-ledger provider-contact path, disclosed and detected by 10C-4 but not disabled or hardened. Their disposition is still unowned."
    ],
    "documentation_fact_corrections": {
        "note": "Recorded for the FINAL document and README phase. Nothing in this list was applied in this ticket - no existing public document was rewritten.",
        "migrations": "25",
        "backend_test_files": "165",
        "backend_suite_at_ee1146b": "3417 passed / 240 skipped",
        "backend_suite_local_at_2bda0e5": "3384 passed / 273 skipped across 165 files (5 files failed on local hook-timeout contention; CI is authoritative)",
        "frontend_tests": "17 test files / 218 tests",
        "playwright": "11 specs / 71 Chromium tests",
        "security": "A bounded security assessment WAS completed (PR #29). README currently contains a FALSE historical statement that the system \"has not been security-audited\" - this must be corrected in the README phase.",
        "live_provider_proof": "One controlled live GreyNoise canary was executed. See docs/evidence/CONTROLLED-LIVE-CANARY-RECORD.md.",
        "master_page_wording": "Do NOT write \"ThreatNeXus makes each exposure an auditable case.\" Finding and Case are distinct. Use conceptually: \"ThreatNeXus turns recurring exposure evidence into persistent, attributed and scored Findings that analysts can triage and, where necessary, progress into auditable investigations.\"",
        "architecture_labelling": "Future documentation must separate CURRENT SHIPPED TOPOLOGY from PILOT RECOMMENDATION from FUTURE / HA DIRECTION, and must never draw the HA architecture without labelling it proposed and unimplemented.",
        "document_control": "New document IDs for the final set. The new Master is NOT a v2 of the old 63-page dossier, which is a historical v1 deliverable. Authorship names the human project team only - no AI, model or tool authorship and no watermark. Review status: \"Submitted for evaluation\"; formal institutional approval: not claimed. Use \"recommended evaluation/adoption path\" or \"a bounded PKCERT pilot could proceed as follows\" - never imply PKCERT has approved a pilot."
    },
    "prior_ticket_evidence": {
        "note": "This file is a rolling single-ticket record: each ticket replaces it. Prior ticket evidence is NOT erased - it is preserved in Git and recoverable at the commits below. This ticket exists partly because that recovery step had never been written down.",
        "TNX-FINAL-FRONTEND-POLISH": "docs/ai/STATE.yaml and docs/ai/HANDOFF.md at 2bda0e5 (PR #30, merged)",
        "TNX-FINAL-SECURITY-PASS": "docs/ai/STATE.yaml and docs/ai/HANDOFF.md at 702afee (PR #29, merged)",
        "TNX-P10C4-CONTROLLED-LIVE-ENRICHMENT-GO-LIVE": "docs/ai/STATE.yaml and docs/ai/HANDOFF.md at 914d582 - now also permanently recorded at docs/evidence/CONTROLLED-LIVE-CANARY-RECORD.md"
    },
    "next_action": "Documentation evidence preparation is COMPLETE. The evidence base for the final document set is in docs/evidence/ and docs/assets/screenshots/final/. NEXT STAGE: draft the ThreatNeXus Official System & Handover Document (18-page target, 20-page hard maximum), then the Analyst & Operations Playbook, then the Knowledge & Defence Handbook, then update README last. Do not begin old-document cleanup until the new set exists.",
    "checkpoint_ref": "",
    "updated_at": "2026-08-19T00:00:00.0000000Z",
    "next_writer": "unassigned"
}
