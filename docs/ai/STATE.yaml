{
    "schema_version":  1,
    "ticket":  "TNX-P7-RELEASE-CANDIDATE",
    "title":  "Phase 7 release-candidate hardening: rate limiting, route authorization census, Docker/offline proof, browser matrix, CI",
    "status":  "complete",
    "owner":  "unassigned",
    "reviewer":  "codex",
    "branch":  "feat/phase-7-release-candidate",
    "worktree":  "C:\\Users\\LENOVO\\Desktop\\ThreatNeXus",
    "starting_commit":  "7aa56ce1ae3690c2eab9a163770a521b83a322f0",
    "goal":  "Continue ThreatNeXus Phase 7 from the paused Claude state and finish the release-candidate hardening deliverable end to end: browser matrix, Docker/offline proof, CI, docs/ai handoff, final checkpoint, and evidence report. Preserve all existing Phase 7 work.",
    "scope":  {
                  "included":  [
                                   "Dependency-free fixed-window rate limiting (auth/upload/provider buckets)",
                                   "Route authorization census with guard metadata (83 routes)",
                                   "Release security assertions (alg:none, secret hygiene, formula-injection forward guard)",
                                   "Phase 7 evaluator (eval:phase7)",
                                   "Docker clean-stack fixes (uploads dir ownership, data mount, seed path, container naming)",
                                   "Offline rehearsal overlay (docker-compose.offline.yml)",
                                   "Chromium browser matrix across four roles and three breakpoints",
                                   "Findings navigation and report-ingestion (upload) E2E coverage",
                                   "CI wiring for eval:phase7 and the disposable rate-limit override"
                               ],
                  "excluded":  [
                                   "Live AI provider",
                                   "Censys, Shodan, Netlas, GreyNoise, VirusTotal, OTX, MISP, Shadowserver ingestion",
                                   "Scheduled provider jobs",
                                   "Automatic notification sending",
                                   "New decorative dashboard features",
                                   "Tailwind/Kokonut/Anime.js or a competing design-system migration",
                                   "PR creation or merge into main"
                               ]
              },
    "completed":  [
                      "Dependency-free fixed-window rate limiter (src/middleware/rateLimit.js): auth/upload/provider buckets, keyed per user then per address, trust-proxy unset and pinned by test, defaults ON except NODE_ENV=test",
                      "Route authorization census (tests/integration/phase7RouteCensus.test.js): all 83 mounted routes authenticate and enforce a capability, or appear on a documented public/self-only exception list",
                      "Release security assertions: alg:none and post-signing payload tampering rejected, secret hygiene swept across success/401/403/404/validation responses, formula-injection forward guard for a CSV export surface that does not yet exist",
                      "Phase 7 evaluator: 8 scenarios / 35 assertions (no-key startup, no-key ingestion, provider unavailability never blocks ingestion, AI off ignores a stale provider name, manual mapping works with AI off, zero outbound fetch calls even with a key set, keys unreachable from the frontend bundle, gold-standard dependency honestly reported ABSENT)",
                      "Fixed three clean-stack Docker defects the proof exposed: EACCES on /app/uploads (root-owned image, node-owned uploads dir only), demo dataset silently empty (added ./data read-only mount, TNX_DATA_DIR-driven fixture path, hard failure instead of a skip-and-zero), and container_name collisions on repeat starts",
                      "Added docker-compose.offline.yml: blackholes the four provider hosts to 0.0.0.0 inside the container while leaving container-to-container and published-port traffic intact",
                      "Root-caused and fixed the paused browser-matrix 429s: the auth limiter's default budget (30/15min) correctly refuses the Chromium suite's dozens of real sign-ins across four roles; added a disposable RATE_LIMIT_AUTH_MAX=1000 override to the CI e2e job's backend-start step, matching the override guidance already documented in docker-compose.yml — production defaults were not weakened",
                      "Extended the Chromium suite from 42 to 45 tests: frontend/e2e/findingsUpload.spec.js adds Findings navigation (list to detail), report ingestion via the real Upload form (proving the dedup/persistence result renders truthfully on a repeat report), and VIEWER refused the ingestion route in place with the session left intact — each asserting zero console errors/warnings",
                      "UI audit pass (no redesign): dashboard/findings/ATT&CK navigator screens verified live at desktop width — PKCERT watermark restrained and clear of evidence text, status badges carry icon+text (never color alone), ATT&CK navigator's honest no-coverage-percentage disclaimer intact; reduced motion, stranded-element, and breakpoint claims rely on the automated suite (motion.spec.js, responsive.spec.js), which passed",
                      "Re-verified the Docker clean-stack proof from a fresh disposable worktree at the final commit: 18 migrations from zero, GET /api/dashboard/overview 401 fail-closed, frontend 200, seed:demo 11 findings / 5 persistent identical online and under the offline overlay, seed:demo idempotent for Finding evidence across repeated runs (case/notification creation is not deduplicated by design — only Findings carry a dedup key), and the default (unoverridden) rate limiter measured refusing at exactly the 31st rapid auth attempt with 429/RateLimit headers",
                      "Ran the full local gate: Prisma validate, migrate-from-zero + no-drift on a fresh database, ATT&CK catalogue verify, backend suite against real PostgreSQL (2848 passed / 177 skipped), all evaluators (phase1, risk, phase2 + mutation, vulnerability + mutation, phase3, phase4, phase5, phase6.3, phase7) all PASS, frontend lint (0, six pre-existing Fast Refresh warnings), frontend unit tests (143/143), frontend build with a clean bundle secret scan",
                      "Pushed feat/phase-7-release-candidate and watched GitHub Actions run 31089169913 to a green conclusion (all required jobs; Mutation and concurrency gates remains manual by design)"
                  ],
    "validation":  {
                       "prisma":  "PASS — schema valid, 18 migrations deploy from zero on two independent fresh databases, no schema drift",
                       "backend":  "PASS — 2848 passed / 177 skipped against real PostgreSQL (local); CI backend job green",
                       "frontend":  "PASS — lint exit 0 with six pre-existing Fast Refresh warnings; 143/143 unit tests; production build clean, bundle secret scan clean",
                       "evaluators":  "PASS — phase1 (9), risk (19), phase2 (22/112) + mutation (6/6), vulnerability (41/992) + mutation (12/12), phase3 (12/151), phase4 (14/151), phase5 (14/150), phase6.3 (13/108), phase7 (8/35)",
                       "browser":  "PASS — Chromium 45/45 locally and in CI against the real REST stack; four roles (ADMIN/ANALYST/REVIEWER/VIEWER); desktop/tablet/mobile; keyboard, reduced motion, findings navigation, report ingestion, denied-route-in-place",
                       "docker_clean_stack":  "PASS — fresh disposable worktree, docker compose up from source, 18 migrations from zero, health-gated startup, seed:demo 11/5 (idempotent for Findings), rate limiter refuses the 31st rapid auth attempt with 429",
                       "docker_offline":  "PASS — docker-compose.offline.yml overlay, four provider hosts blackholed to 0.0.0.0, seed:demo produces the identical 11/5 result with zero outbound calls",
                       "ci_workflow":  "PASS — GitHub Actions run 31089169913 at commit ea97745, conclusion success, all required jobs green (hygiene, schema, backend, frontend, evaluators, e2e); Mutation and concurrency gates (manual) intentionally not triggered",
                       "secret_hygiene":  "PASS — no live key read, printed, transmitted, or committed; backend/.env never opened this session"
                   },
    "known_issues":  [
                         "Finding closure still has no production write path (carried over from Phase 6.3), so recurrence/reopen remains evaluator-proven but unreachable through the running UI.",
                         "Chromium is the committed browser gate; Firefox and WebKit are not claimed.",
                         "seed:demo is idempotent for Finding evidence (11/5 across repeated runs) but not for demonstration Cases/Notifications, which have no dedup key by design — re-running the seed against a stack that already has demo data adds another case/notification rather than skipping. This is intentional per the locked dedup-key scope (Findings only), not a defect.",
                         "The Phase 7 evaluator's gold-standard dependency is honestly reported ABSENT: two named team members labelling 50-100 findings independently is a human deliverable tracked in BUILD_PLAN, not something this session could produce.",
                         "docs/codex/ remains untracked and untouched throughout this session, per the one-writer boundary."
                     ],
    "next_action":  "None required to close this ticket. If Phase 8 or further hardening begins, start from a fresh writer-lock acquisition against this checkpoint.",
    "checkpoint_ref":  "ea977459313c5c2e257a823441e303ee5fb45139",
    "updated_at":  "2026-08-06T09:45:00.0000000Z",
    "next_writer":  "unassigned"
}
