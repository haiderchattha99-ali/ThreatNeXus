{
    "schema_version": 1,
    "ticket": "TNX-P10A1-ENRICHMENT-ORCHESTRATION-FOUNDATION",
    "title": "Phase 10A-1 - inert enrichment-orchestration foundation (Codex review pass 1 resolved)",
    "status": "awaiting_review",
    "owner": "claude",
    "reviewer": "codex",
    "branch": "feat/phase-10a1-enrichment-orchestration-foundation",
    "worktree": "F:\\AI-Worktrees\\ThreatNeXus\\phase-10a1",
    "starting_commit": "3638a3983ec7e45e7dfc70cb9583e7b6a5cd8912",
    "goal": "Deliver the inert Phase 10A-1 enrichment-orchestration foundation - one additive migration (5 tables, 9 enums, 7 CHECK constraints), subject model, applicability router, run/item/job services, reconciliation service (callable, unscheduled), async run/summary/usage APIs, default-off configuration, ingestion integration and backend tests - and resolve every blocker from the first Codex independent review. Zero provider calls, zero quota reservations, zero attempt/usage rows, no worker, no frontend change.",
    "scope": {
        "included": [
            "One additive migration: 5 tables, 9 enums, 7 CHECK constraints. No existing column altered. Still 24 migrations after the correction pass - no 25th was added",
            "EnrichmentSubjectType + provider/subject compatibility (5 IPv4 providers, nvd CVE-only), canonicalization before hashing",
            "queryIdentityHash (work identity) kept SEPARATE from requestScopeHash (ask identity); bounded Idempotency-Key handling (max 128 UTF-8 bytes, no control chars, SHA-256 digest only)",
            "Applicability router with explicit precedence; force bypasses freshness only",
            "Run/item/lookup-job repository + services, run-state recomputation, shared activeLookupKey dedup",
            "CONVERGENT item materialization: every idempotent replay attempts the complete expected routed-target set and writes only what is missing",
            "Delegated providers (abuseipdb, nvd) scheduled through the CANONICAL queue services and linked; no RUN_DELEGATED job is ever created without exactly one delegate FK",
            "Reconciliation service - callable in tests, scheduled by nothing",
            "Async run API on the approved v2.1 paths (/api/findings/:id/enrichment/runs) with the closed CREATED/ALREADY_RUNNING/SKIPPED outcome vocabulary, and a truthful usage API with PHASE_10_RESERVATIONS / PARTIAL / reservationsActive=false / excludedPaths",
            "AUTO_ENRICHMENT_ENABLED=false, ENRICHMENT_WORKER_ENABLED=false, every automatic budget 0, manual budgets unlimited-but-validated",
            "Ingestion integration behind the switch + one additive `enrichment` response block carrying a closed `state` code, surfaced over HTTP by the upload controller",
            "Audit events for the inert milestone (counts and closed codes only)"
        ],
        "excluded": [
            "worker.js or any worker loop; provider executors, factories, lookups, fetch calls - a static inertness test proves the package imports none of them",
            "Quota reservation SQL/service; ProviderLookupAttempt rows; ProviderDailyUsage rows - all zero by design",
            "MANUAL_DIRECT wiring; targeted IOC runner changes; vulnerability runner changes; automatic NVD/EPSS/KEV execution",
            "Frontend changes (git status frontend/ empty), cancellation, Risk v1 changes, live provider smoke tests",
            "A 25th migration for a RUN_DELEGATED delegate-FK CHECK constraint - the invariant is enforced in code and asserted per job in the real-PG suite; promote it when 10A-2 next touches the migration set",
            "PR creation and any merge to main - explicitly out of scope",
            "backend/.env - never opened, read, printed or referenced; only .env.example consulted",
            "The primary checkout C:\\Users\\LENOVO\\Desktop\\ThreatNeXus and its Phase 9 documentation/presentation work - never touched, never switched, never stashed",
            "docs/codex/ - not present in this worktree and never created"
        ]
    },
    "codex_review_pass_1": {
        "verdict_received": "NOT READY - six blockers",
        "status": "all six resolved on this branch, each with a regression test",
        "findings": [
            "1. API contract: replaced the invented /:id/enrichment-runs with the approved /:id/enrichment/runs (POST, GET) and /:id/enrichment/runs/:runId (GET), with NO alias retained; replaced `created: false` under 202 with a closed outcome - CREATED=202, ALREADY_RUNNING=200 returning the existing run, SKIPPED=200. Body shape pinned field-by-field in tests.",
            "2. Upload contract: renamed the additive `autoEnrichment` block to `enrichment` with a closed state vocabulary (AUTOMATIC_DISABLED / NO_FINDINGS / RECORDED / PARTIAL); `executed` stays false. The block had never been surfaced over HTTP at all, so reportIngestionController now returns it additively alongside findingCounts. Pre-Phase-10 fields unchanged.",
            "3. Partial-materialization recovery: replaced the `items.length === 0` guard with convergent materialization that reattempts the complete expected target set on every replay, skips what is present, relies on the existing unique for concurrent racers, and never overwrites an existing decision.",
            "4. Unlinked delegated jobs: delegates are now created-or-found through enrichmentQueueService / vulnerabilityQueueService and linked; a fresh-answer report downgrades the item to SKIPPED_CACHED and a refusal/failure to SKIPPED_EXECUTION_UNAVAILABLE + DELEGATE_UNAVAILABLE, both with NO job created.",
            "5. HTTP/RBAC contract tests: new backend/tests/integration/phase10a1RouteAuthorization.test.js, 37 Supertest cases over the mounted application. No existing authorization test was weakened.",
            "6. AI-team state: this file rewritten to the current ticket only."
        ],
        "extra_defects_found_and_fixed": "phase10a1Orchestration.test.js deleted RawReport WHERE sourceFileName LIKE 'p10a1-%', which also matched phase10a1IngestionDefaultOff.test.js's reports and failed on their child foreign keys when the two ran concurrently; and phase10a1IngestionDefaultOff.test.js used 192.0.2.181, inside the entire /24 that ownershipConcurrency.test.js blanket-deletes by indicator prefix. Both suites now use ranges and delete patterns no other suite claims."
    },
    "validation": {
        "prisma": "format + validate pass; migrate deploy from zero applies 24/24 on a fresh database; migrate diff --exit-code reports no difference (exit 0)",
        "live_schema": "direct pg_catalog query on the freshly migrated database: 24 migrations, 5 Phase-10 tables, 9 Phase-10 enums, 7 named CHECK constraints, 0 ProviderLookupAttempt rows, 0 ProviderDailyUsage rows, 0 RUN_DELEGATED jobs without a delegate FK",
        "unit": "101 tests across 6 suites, including the static inertness gate that fails on any forbidden import, fetch or lookup call, attempt or usage write, raw SQL, timer, or worker in the package - still green with the two new canonical-queue imports",
        "real_pg_constraints": "9 tests proving 8 independent rejections across the 7 constraints, each asserting the constraint NAME",
        "real_pg_orchestration": "9 tests (T-23 scope separation, concurrent collapse, distinct scopes, shared job across Findings, policy skips, automatic budget 0, 3 verified CVEs, provider-text exclusion, audit payload safety)",
        "real_pg_default_off": "3 tests (off = zero Phase-10 rows and enrichment.state AUTOMATIC_DISABLED; on = durable records, jobs non-terminal and never queried, zero attempts, zero usage; identical re-upload creates no second run)",
        "real_pg_delegate_recovery": "NEW - 9 tests: crash recovery fills every missing item exactly once while preserving the surviving item and job identities; two concurrent replays converge; IOC delegate created when absent; existing active IOC delegate reused with no second row; a terminal prior IOC row is neither linked nor blocking and its history survives; a scheduling refusal yields SKIPPED_EXECUTION_UNAVAILABLE and no job; NVD delegate created and linked; three verified CVEs keep three distinct delegates; the ADMIN batch's active-job uniqueness is never bypassed. Every case re-asserts zero attempts, zero usage, nothing queried, and exactly one delegate FK per RUN_DELEGATED job",
        "http_rbac": "NEW - 37 Supertest tests: 401 unauthenticated on every route; ADMIN/ANALYST create, REVIEWER/VIEWER 403; all four roles read; usage is ADMIN-only; 403 never downgraded to 401; slash paths served and hyphenated paths 404; 202 CREATED / 200 ALREADY_RUNNING / 200 SKIPPED; malformed provider scope, force and Idempotency-Key all 400; a run belonging to another Finding 404; no job id, hash, claim token or foreign subject in any body",
        "upload_http_contract": "reportUploadRoute.test.js proves enrichment.state === AUTOMATIC_DISABLED over real HTTP on the default deployment, and its response allow-list test was EXTENDED (not relaxed) to pin the new block's own keys",
        "full_suite": "3415 passed, 2 skipped, 0 failed (156 files) on a fresh database at --maxWorkers=3, with every provider key empty",
        "evaluators": "eval:phase1 PASS, eval:risk PASS (19/19), eval:phase2 PASS (22/112), eval:phase3 PASS (12/151), eval:vulnerability PASS (41/992), eval:phase4 PASS (14/151), eval:phase5 PASS (14/150), eval:phase6.3 PASS (13/108), eval:phase7 PASS (8/35)",
        "risk_unchanged": "RISK_CONFIGURATION_VERSION v1.0.0 and RISK_ALGORITHM_VERSION risk-additive-bucketed-v1 unchanged; git status backend/src/services/risk/ is empty; eval:risk re-proves the fingerprint",
        "secret_scan": "CI's own credential-shaped-literal pattern set run over src, tests, docs and README including the two new files: clean. No tracked .env. Provider key names appear only in enrichmentOrchestrationConfig.js, asserted by the inertness test",
        "frontend": "untouched - git status frontend/ is empty",
        "no_real_env_read": "PASS - backend/.env never opened, read, printed or referenced",
        "primary_checkout": "untouched - C:\\Users\\LENOVO\\Desktop\\ThreatNeXus stayed on docs/phase-9c-pkcert-technical-dossier with its unrelated Phase 9 work intact",
        "ci": "pending - filled in by the post-push checkpoint commit"
    },
    "known_issues": [
        "MANDATORY PRE-10A2 BLOCKER: the existing IOC runner owns claimPendingJob and calls provider.lookup() internally. A targeted runner/hook contract must be designed AND independently reviewed before 10A-2 - see docs/ai/HANDOFF.md for the seven guarantees it must provide.",
        "MANUAL_DIRECT, ProviderLookupAttempt and ProviderDailyUsage are defined and constraint-proven but deliberately unused or empty in 10A-1 - they exist so the accounting contract is provable before any code can spend quota. SKIPPED_EXECUTION_UNAVAILABLE is NO LONGER in this list: the delegate correction made it a live, tested decision.",
        "ENRICHMENT_WORKER_ENABLED is declared and validated but consumed by nothing; there is no worker to enable.",
        "A RUN_DELEGATED job without a delegate FK is prevented in code and asserted per job in the real-PG suite, not by a database CHECK constraint - adding one would mean a 25th migration and a change to CI's frozen list, which this correction pass was scoped out of.",
        "Freshness is read one query per routed target (bounded by provider scope plus one per verified CVE). Marked with a ponytail: comment naming the batching upgrade path.",
        "The Phase 10 Plan v2 and v2.1 documents exist in no file on disk or on the F drive. The route paths, the CREATED/ALREADY_RUNNING/SKIPPED outcome vocabulary and the enrichment.state codes now implemented come from the Codex review's explicit restatement of the approved contract. If v2/v2.1 names anything differently, reconcile the NAMES - the behaviour and status codes match what the review specified.",
        "Local full-suite runs at DEFAULT parallelism produce 5s-timeout failures in unrelated pre-existing suites (caseWorkflowConcurrency, vulnerabilityPacketBApplication, vulnerabilityReleaseWorkflow). Proven unrelated: they import no changed module, they pass in isolation, and the same failures still occur with every Phase-10 file excluded. A fresh database at --maxWorkers=3 is fully green.",
        "The second independent review has not happened yet - per CLAUDE.md this must not be treated as final."
    ],
    "next_action": "Codex SECOND independent review of feat/phase-10a1-enrichment-orchestration-foundation, against the six pass-1 findings recorded above. Do NOT open a PR, do NOT merge main, do NOT begin 10A-2. After that review passes, design the targeted IOC runner/hook contract recorded in docs/ai/HANDOFF.md and have that design independently reviewed before writing any 10A-2 execution code.",
    "checkpoint_ref": "0b95788",
    "updated_at": "2026-08-12T04:30:00.000Z",
    "next_writer": "codex",
    "current_work": "",
    "files_changed": [
        "backend/prisma/schema.prisma",
        "backend/prisma/migrations/20260811162611_add_phase10a1_enrichment_orchestration/migration.sql",
        "backend/src/services/enrichmentOrchestration/ (10 modules)",
        "backend/src/controllers/enrichmentOrchestrationController.js",
        "backend/src/controllers/reportIngestionController.js",
        "backend/src/routes/enrichmentOrchestrationRoutes.js",
        "backend/src/routes/enrichmentUsageRoutes.js",
        "backend/src/app.js",
        "backend/src/config/env.js",
        "backend/src/services/ingestion/reportIngestionService.js",
        "backend/.env.example",
        ".github/workflows/ci.yml (frozen migration list -> 24)",
        "backend/tests/unit/ (6 suites)",
        "backend/tests/integration/phase10a1Constraints.test.js",
        "backend/tests/integration/phase10a1Orchestration.test.js",
        "backend/tests/integration/phase10a1IngestionDefaultOff.test.js",
        "backend/tests/integration/phase10a1DelegateRecovery.test.js (new)",
        "backend/tests/integration/phase10a1RouteAuthorization.test.js (new)",
        "backend/tests/integration/reportUploadRoute.test.js",
        "README.md, docs/PROVIDER_GUIDE.md, docs/DEPLOYMENT.md, docs/ai/SECURITY.md, docs/ai/HANDOFF.md, docs/ai/STATE.yaml"
    ]
}
