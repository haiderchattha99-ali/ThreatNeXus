{
    "schema_version":  1,
    "ticket":  "TNX-P10C4-CONTROLLED-LIVE-ENRICHMENT-GO-LIVE",
    "title":  "Controlled live enrichment go-live contract (implementation checkpoint: read-only canary preflight, contract v2)",
    "status":  "implementation_checkpoint",
    "owner":  "claude",
    "reviewer":  "security-reviewer + backend-logic-reviewer + software-architect (independent-gate substitute; Codex FAILED - usage limit, resets 2026-08-21). READY: 0 P0, 6 P1 raw / 4 distinct (ALL CLOSED in revision 2), 16 P2 (all closed), 6 P3 (all closed).",
    "branch":  "feat/phase-10c4-controlled-live-enrichment",
    "worktree":  "F:\\AI-Worktrees\\ThreatNeXus\\phase-10c4",
    "starting_commit":  "0f8183518c485cc3be5ee29b5a2ea5be7a37ca24",
    "goal":  "Tier-3 design/contract gate ONLY. Define the smallest safe path from the currently implemented enrichment system to controlled live-provider execution proof, without turning it into a production-deployment redesign. NO product code, NO live provider contact, NO production enablement, NO secret values, NO start on 10C-5.",
    "current_work":  "",
    "files_changed":  [
                          "docs/ai/PHASE-10C4-CONTROLLED-LIVE-ENRICHMENT-CONTRACT.md",
                          "docs/ai/DECISIONS.md",
                          "docs/ai/STATE.yaml",
                          "docs/ai/HANDOFF.md",
                          "backend/src/scripts/enrichmentCanaryPreflight.js",
                          "backend/tests/unit/enrichmentCanaryPreflight.test.js",
                          "backend/package.json",
                          "docs/OPERATIONS_RUNBOOK.md",
                          "docs/PROVIDER_GUIDE.md"
                      ],
    "scope":  {
                  "included":  [
                                   "Go-live DEFINITION (contract section 4): one operator-authorized MANUAL-lane run, one provider, one subject, budget of exactly one reservation, disposable LOCAL stack, durable ledger evidence, verified return to default-off. Explicitly not a deployment, not a continuously-running worker, not AUTOMATIC-lane execution",
                                   "Grounded default-off model (section 3): eight independent gates on the Phase-10 path, verified in source at 0f81835",
                                   "Section 3.2 (NEW in revision 2, the single most important correction): the four legacy synchronous provider routes contact providers OUTSIDE Phase-10 accounting - no reservation, no attempt row, no lookupWithBound - and are armed by the credential alone, independent of ENRICHMENT_WORKER_ENABLED",
                                   "Provider selection: greynoise ONLY (free Community API, one GET, one header, 404 -> NOT_FOUND first-class, DIRECT lane so it proves the shared direct-lane path). Excluded with reasons: shodan/netlas/censys (paid or credential-complex), abuseipdb (delegated lane, deliberately not first), nvd (structurally DELEGATED_BATCH_REQUIRED)",
                                   "Safe subject: 1.1.1.1, reusing the ALREADY-APPROVED in-repo Phase-8D smoke subject rather than introducing new third-party exposure",
                                   "Credential boundary (section 8): nine bindings, incl. the new no-process-environment-dump rule (docker inspect / compose config / printenv / /proc/<pid>/environ / shell history all hold the plaintext key)",
                                   "Deterministic preflight (section 12.2): 17 assertions, bound to the worker's OWN process environment; P5 (budget strictly === 1) is load-bearing because a BLANK manual budget resolves to null = UNLIMITED",
                                   "One bounded canary procedure (section 13, C1-C10), evidence set (13.1) and a five-state contact vocabulary (13.2)",
                                   "Kill switch / rollback (section 11) with BINDING step ordering - evidence must be extracted before the volume is destroyed",
                                   "14 binding invariants; I-02 and I-04 deliberately PATH-SCOPED, not system-wide",
                                   "10C-5 sequencing decision (section 17): 10C-4 MAY proceed first; 10C-5 remains required before unattended/production enablement. Section 17.4 lists five residual gaps carried forward",
                                   "Implementation surface (section 14): Shape A - procedure/evidence only. One new read-only preflight script + its unit tests + docs. NO product code in the execution path, zero schema change",
                                   "D-P10C4-01 in DECISIONS.md"
                               ],
                  "excluded":  [
                                   "ALL product code - nothing under backend/src or frontend/src was written or modified this session",
                                   "Any live provider call, any provider contact, any quota spend - none occurred",
                                   "Any production/staging deployment, cloud infrastructure, hosting migration, CI integration",
                                   "Any credential value - none was requested, read, printed, or reconstructed",
                                   "10C-5 (provider response-body size hardening) - explicitly NOT started, recorded as remaining hardening",
                                   "Retirement or hardening of the legacy synchronous provider routes - disclosed and detected in this contract, but unowned and left to a future ticket",
                                   "The targeted/delegated (abuseipdb) canary - candidate successor ticket",
                                   "Credential delivery/egress in a hosted environment, and provider-account-level behaviour - named residual gaps, currently unowned"
                               ]
              },
    "completed":  [
                      "Resolved the exact live base from Git rather than a stated SHA: origin/main fetched to 0f8183518c485cc3be5ee29b5a2ea5be7a37ca24 (merge of PR #25). Created an isolated worktree at F:\\AI-Worktrees\\ThreatNeXus\\phase-10c4 on a new branch from that exact commit, acquired the writer lease (49c3e199), and verified the protected primary checkout at C:\\Users\\LENOVO\\Desktop\\ThreatNeXus was never written to",
                      "Grounded the real system in source rather than from the prior contracts' claims: the eight-gate default-off chain, the worker start site (backend/server.js:17-25, read-once-at-boot, sole call site in all of backend/), DIRECT vs DELEGATED provider split, the atomic reservation+ledger transaction, contactedProvider written BEFORE the call, the no-refund conservative accounting, trigger->lane mapping (enrichmentRunService.js:701), force semantics, docker/env passthrough, CI defaults, and the pre-existing Phase-8D live smoke scripts",
                      "Found the design's decisive fact independently: DEFAULT_MANUAL_DAILY_BUDGET is null and null means UNLIMITED, so a blank GREYNOISE_MANUAL_DAILY_BUDGET is not zero - and every analyst-triggered run is MANUAL lane. This single fact is why the contract's only implementation surface is a preflight that refuses unless the budget resolves to exactly 1",
                      "Established that the existing npm run smoke:* scripts prove the provider ADAPTER only - they create no job, reserve no quota, write no attempt row, and never touch the worker. That gap is precisely what 10C-4 exists to close",
                      "Split preflight: eight high-risk surfaces counted, decided ONE ticket / no split - they are eight facets of one ceremony, none independently landable, and no independent risky system is created",
                      "Wrote contract revision 1 and dispatched the smallest sufficient Tier-3 reviewer set (three non-overlapping read-only reviewers). Attempted Codex as the policy-mandated independent-provider gate for a go-live boundary; it FAILED on usage limit (resets 2026-08-21) and this was recorded honestly rather than silently substituted",
                      "Reviewers returned 0 P0 and 6 P1 (4 distinct). All three independently found the same most-important defect - the legacy synchronous provider route as an off-ledger contact path. Verified that finding against source myself before accepting it, and likewise verified the other three P1s and the FindingEnrichmentRun naming fact rather than taking reviewer output at face value",
                      "Closed all 6 P1, 16 P2 and 6 P3 in one consolidated revision-2 fix pass. Two revision-1 claims were outright FALSE and are corrected in place with the error acknowledged rather than quietly deleted: preflight P4 was unsatisfiable (isProviderCredentialConfigured('nvd') returns true unconditionally, so the preflight would have refused every canary), and P12 was called 'the sharpest single assertion' when EXECUTION_PAUSED actually masks every budget state below it, making P12 blind to a blank/unlimited budget",
                      "IMPLEMENTATION CHECKPOINT: recovered the dead writer lease (previous PID 24828/powershell confirmed dead by process-identity evidence, not lease age) via continue-task.ps1 -RecoverDeadLease, new lease f9b02d0a superseding 49c3e199. Verified HEAD is exactly the design checkpoint c92cf86 on the correct branch with a clean working tree before writing anything",
                      "Wrote backend/src/scripts/enrichmentCanaryPreflight.js implementing all seventeen P1-P17 assertions as a pure evaluateCanaryPreflight(config, dbFacts, rawEnv, now) function plus a gatherDbFacts(prisma) DB-composition layer and a require.main-guarded CLI entrypoint. Composes isProviderCredentialConfigured, resolveProviderReadiness, KNOWN_PROVIDERS, QUOTA_LANES, NON_TERMINAL_JOB_STATES and utcUsageDate from the existing orchestration package rather than reimplementing any of their logic. Imports no provider/execution-service module anywhere in the file",
                      "Established the P15 'disposable-canary database name prefix' convention referenced but never defined anywhere in the repository or the contract: the resolved DATABASE_URL's database name must contain the literal substring 'canary' (documented in docs/OPERATIONS_RUNBOOK.md as an operator naming convention, not a cryptographic guarantee)",
                      "Wrote backend/tests/unit/enrichmentCanaryPreflight.test.js: 63 deterministic tests, no real database, no network. Red-checks every one of P1-P17 including the load-bearing cases (P5 blank/null/'unlimited'/0/>1 all fail; P4 green baseline proves nvd's exemption makes the assertion satisfiable at all; P13 catches a legacy-path audit row even with zero attempt-shaped evidence; P14 near-UTC-midnight fails; P12-vs-P5 regression test proving EXECUTION_PAUSED masking does not let a bad budget slip through). Proves no-network-contact two ways: a static import-boundary check against the script's own source text, and a global.fetch spy that must never fire",
                      "Found and fixed one real defect during red-checking: resolveProviderReadiness THROWS (by design) on a non-null/non-integer dailyBudget, so passing the literal string 'unlimited' straight through in P12 crashed the whole preflight instead of failing cleanly. Hardened P12 to coerce any non-null/non-integer budget to null before calling resolveProviderReadiness — P5 still reports the real defect independently; P12 now degrades to a clean FAIL instead of throwing",
                      "Added the 'preflight:canary' script entry to backend/package.json alongside the existing smoke:* entries; confirmed it is not referenced by npm test or any CI workflow file",
                      "Added 'Controlled live canary (Phase 10C-4)' to docs/OPERATIONS_RUNBOOK.md (command, what PASS/does-not-mean, safe environment prep, one-call cap, secret boundary, worker restart requirement, legacy-route caveat, rollback requirement, 10C-5 boundary) and a 'Phase 10C-4 — which provider is approved for live proof' section to docs/PROVIDER_GUIDE.md",
                      "Focused validation run and green: the new 63-test suite; the three directly-composed existing suites (enrichmentOrchestrationConfig.test.js, enrichmentProviderReadiness.test.js, enrichmentOrchestrationInertness.test.js, 57 tests, zero regression); prisma validate (schema unchanged, zero migration touched); git diff --check clean. Did NOT stand up a disposable Postgres to smoke-run the script live — deliberately out of this checkpoint's scope: gatherDbFacts's Prisma composition is exercised deterministically against a fake Prisma double in the new suite, and a real-Postgres dry run belongs to the later C1-C10 operator-executed canary boundary, not this preflight-authoring checkpoint"
                  ],
    "validation":  {
                       "passing":  [
                                       "Base resolved from Git and worktree verified at the exact SHA on the correct branch",
                                       "Primary checkout verified untouched throughout (unchanged dirty-file count, session start to end)",
                                       "Writer lease recovered through the canonical process-identity-based recovery path (continue-task.ps1 -RecoverDeadLease) after confirming the recorded PID was genuinely dead, not merely aged",
                                       "npx vitest run tests/unit/enrichmentCanaryPreflight.test.js -> 63/63 passing (deterministic, no real database, no network)",
                                       "npx vitest run tests/unit/enrichmentOrchestrationConfig.test.js tests/unit/enrichmentProviderReadiness.test.js tests/unit/enrichmentOrchestrationInertness.test.js -> 57/57 passing, zero regression from the directly-composed existing suites",
                                       "npx prisma validate -> schema valid, zero schema/migration change (only backend/src/scripts, backend/tests/unit, backend/package.json and four docs files touched)",
                                       "git diff --check -> clean, no whitespace errors",
                                       "Zero product code touched in the execution path - no enrichmentWorker.js, no *ExecutionService.js, no *Provider.js, no routes/controllers, no frontend, no Prisma schema/migrations",
                                       "Zero live provider contact, zero quota spend, zero secret values read/printed/requested this session",
                                       "No-network-contact proven for the new script itself: a static import-boundary test asserts the source never references any provider/execution-service module, and a global.fetch spy test asserts it is never called by gatherDbFacts or evaluateCanaryPreflight"
                                   ],
                       "failing":  [],
                       "not_applicable_this_gate":  [
                                       "The live canary itself (contract section 13, C1-C10) and its post-live evidence (section 13.1) - a LATER, separate, operator-executed boundary explicitly out of scope for this implementation checkpoint",
                                       "A real-Postgres dry run of the preflight script (npm run preflight:canary against a live disposable database) - the DB-composition logic (gatherDbFacts) is proven deterministically against a fake Prisma double instead; standing up a disposable stack belongs to the C1 step of the later canary boundary"
                                   ],
                       "independent_review":  "PENDING for this implementation checkpoint - not yet dispatched. The design/contract gate's 3-reviewer pass (security-reviewer, backend-logic-reviewer, software-architect, 0 P0, all P1/P2/P3 closed in revision 2) remains valid for the CONTRACT; the new preflight SCRIPT/test/docs surface has not yet had its own independent review pass. Codex remained unavailable in the design gate (usage limit, resets 2026-08-21) and was not re-attempted this session per the writer-boundary instruction to avoid spawning another coding/review writer mid-checkpoint.",
                       "primary_checkout":  "VERIFIED UNTOUCHED - only F:\\AI-Worktrees\\ThreatNeXus\\phase-10c4 was written to this session"
                   },
    "known_issues":  [
                         "NEW CONVENTION ESTABLISHED THIS CHECKPOINT (not a defect, but worth a reviewer's eyes): contract v2's P15 requires 'the database name carries the required disposable-canary prefix' but that prefix/marker is not defined anywhere else in the repository or the contract text. This session established and documented the convention that the resolved DATABASE_URL's database name must contain the literal substring 'canary' (docs/OPERATIONS_RUNBOOK.md, preflight P15). This is a naming-convention sanity check, not a cryptographic guarantee, and does not itself verify 'this is the same process that will boot the worker' - only the execution-binding instruction (run the preflight inside the worker's own container) establishes that.",
                         "PENDING GATE (not a defect): Codex, the preferred independent cross-provider reviewer, was unavailable (usage limit, resets 2026-08-21). TEAM-WORKFLOW.md routes go-live to an independent-provider gate, so this gap is material. software-architect carried that scope as the canonical internal fallback - the same substitution 10C-3 made for the same reason. A Codex pass before implementation begins would be strictly additive confirmation, not a reopening: no P0 was found by any reviewer and every P1 is closed. This gap now also covers the new preflight implementation surface, which has had no independent review pass at all yet.",
                         "DISCLOSED RESIDUAL (contract section 3.2, unowned): the four legacy synchronous provider routes (POST /api/findings/:id/enrichment/{greynoise,censys,shodan,netlas}) contact providers outside Phase-10 accounting entirely and are armed by the credential alone. This contract discloses and DETECTS them; it does not disable, retire, or harden them. No frontend code calls them, so reaching one requires a deliberate hand-made authenticated request. Their disposition belongs to a future ticket.",
                         "DISCLOSED RESIDUAL (contract section 17.4, unowned): credential delivery and egress in any non-local environment, and provider-account-level behaviour (source-IP key binding, shared per-account rate accounting), cannot be established by a local-only canary. Prerequisites of any deployment ticket.",
                         "DISCLOSED RESIDUAL: 10C-5 (provider response-body SIZE bounding) remains open. The worker path bounds the worker's DECISION but does not cancel the read - Promise.race cancels nothing and no signal is threaded through. Required before unattended/production enablement, batch size > 1, or any provider with large/variable responses.",
                         "DISCLOSED RESIDUAL: closing 10C-4 confers NO live proof on abuseipdb or the targeted/delegated lane - against the clean database the preflight requires, those worker passes execute over zero rows, vacuously.",
                         "CARRIED FORWARD from 10C-3 (unverified, still worth one line): confirm the CI job running backend/tests/integration/phase10c3UsageService.test.js sets TEST_DATABASE_URL, or the suite self-skips and silently drops real-Postgres coverage while reporting green."
                     ],
    "next_action":  "STOP - implementation checkpoint complete: the contract-v2 preflight harness, its focused tests, package script and operator docs are written and green. Do NOT contact any provider, do NOT enable the worker, do NOT run the live canary, do NOT start 10C-5. Remaining before the live-canary boundary (contract section 13, C1-C10) can open: (1) an independent review pass of this new preflight/test/docs surface (Codex still pending, resets 2026-08-21 - or an internal fallback reviewer per TEAM-WORKFLOW.md), (2) a human operator decision to authorize the actual canary, (3) standing up the disposable local stack per docs/OPERATIONS_RUNBOOK.md 'Controlled live canary (Phase 10C-4)' and running npm run preflight:canary inside that stack's own backend container before any credential is set.",
    "checkpoint_ref":  "",
    "pr_url":  "",
    "updated_at":  "2026-08-17T00:00:00.0000000Z",
    "next_writer":  "unassigned"
}
