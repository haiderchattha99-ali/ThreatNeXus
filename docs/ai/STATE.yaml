{
    "schema_version":  1,
    "ticket":  "TNX-P8B-CENSYS-PROVIDER",
    "title":  "Phase 8B - Censys live internet-exposure/attack-surface provider integration",
    "status":  "complete",
    "owner":  "unassigned",
    "reviewer":  "codex",
    "branch":  "feat/phase-8b-censys-provider",
    "worktree":  "C:\\Users\\LENOVO\\Desktop\\ThreatNeXus",
    "starting_commit":  "4fb373c",
    "goal":  "Build exactly one new live provider adapter, Censys, using the existing provider foundation and NVD/AbuseIPDB patterns (registries, config safety, audit logging, Phase 7 shared rate limiting) without rebuilding or duplicating any of it. No live calls in tests/CI, no secret leakage, quota enforced, audit logged, docs updated. Do not integrate Shadowserver, Shodan, Netlas, GreyNoise, VirusTotal, OTX, or MISP.",
    "scope":  {
                  "included":  [
                                   "CensysEnrichment Prisma model + migration (own table, not a bolt-on to IocEnrichment; no queue/lease/retry — synchronous terminal rows only)",
                                   "censysConfig.js / censysTypes.js / censysProvider.js (self-contained adapter mirroring abuseIpdbProvider.js's defensive HTTP shape)",
                                   "censysExecutionService.js: audited, synchronous, human-triggered lookup (5 audit actions: attempted/succeeded/failed/unavailable/rate_limited)",
                                   "GET+POST /api/findings/:id/enrichment/censys, reusing existing read:findings / trigger:finding-enrichment capabilities and the existing providerRateLimiter budget",
                                   "Dashboard sections.providers.exposure + Settings.jsx/DashboardSections.jsx rendering it via the existing CONFIG_STATE/PROVIDER_STATE dictionaries (no new UI vocabulary)",
                                   "Opt-in manual live smoke script (LIVE_CENSYS_SMOKE=1, npm run smoke:censys, lookup against 1.1.1.1, never run in CI)",
                                   "41 new/updated tests: censysProvider.test.js (15), censysEnrichmentRouteAuthorization.test.js (14), censysLiveSmoke.test.js (3), phase8bCensysProviderEvidence.test.js (7), plus 2 new phase7RateLimiting.test.js cases proving shared quota",
                                   "Docs: README, docs/ai/SECURITY.md ('Censys - the second live provider' subsection), backend/.env.example, docs/ai/HANDOFF.md, docs/ai/STATE.yaml",
                                   "Merged the prior TNX-P8-PROVIDER-EVIDENCE-GAPFILL branch into main first (PR #9), per the user's explicit start-gate answer, so this branch starts from an updated main"
                               ],
                  "excluded":  [
                                   "Shadowserver, Shodan, Netlas, GreyNoise, VirusTotal, OTX, MISP integration",
                                   "Live AI provider",
                                   "Scheduled/queued Censys jobs — this phase is explicitly synchronous, human-triggered only",
                                   "A unified cross-cutting provider registry spanning IOC/vulnerability/exposure — three separate, intentionally un-unified registries by design",
                                   "PR creation or merge into main (for THIS ticket's branch — the gap-fill PR #9 merge was separately authorized by the user)"
                               ]
              },
    "completed":  [
                      "Verified the mandatory start gate: fetched origin, confirmed the prior gap-fill branch was NOT merged into main, stopped and reported per the user's own rule 4, got explicit direction to open PR #9 and merge it, then branched feat/phase-8b-censys-provider from the resulting updated main (4fb373c)",
                      "Prisma: CensysEnrichment model + enum, additive-only migration 20260807000000_add_phase8b_censys_exposure_enrichment, verified against a real disposable PostgreSQL instance (migrate deploy from zero, prisma validate, migrate diff --exit-code shows zero drift)",
                      "censysConfig.js / censysTypes.js / censysProvider.js: full adapter with Basic Auth, bounded timeout+caller-signal composition, 401/403/404/429/5xx/timeout/malformed/unreachable all mapped to the same closed PROVIDER_ERROR_CODES vocabulary iocEnrichmentTypes.js and vulnerabilityTypes.js already use; verified end-to-end with a hand-run script before writing tests",
                      "censysExecutionService.js: resolves Finding indicator, calls the provider, persists one CensysEnrichment row, writes an attempted+outcome audit pair (5 actions), never throws for an expected provider outcome",
                      "censysEnrichmentController.js + censysEnrichmentRoutes.js, mounted in app.js: GET/POST /api/findings/:id/enrichment/censys reusing read:findings / trigger:finding-enrichment and the existing providerRateLimiter",
                      "backend/src/scripts/censysLiveSmoke.js (npm run smoke:censys): opt-in LIVE_CENSYS_SMOKE=1, one lookup against 1.1.1.1 (Cloudflare public DNS, permanent public infrastructure), never prints credentials",
                      "Dashboard: operationalOverviewService.js buildProvidersSection now queries CensysEnrichment and returns sections.providers.exposure (CONFIGURED/NOT_CONFIGURED + freshness); Settings.jsx and DashboardSections.jsx ProviderFreshness fold the new array into their existing rendering with zero new UI vocabulary",
                      "Fixed two pre-existing test mock clients (operationalOverviewService.test.js, riskFactorPressure.test.js) that needed a censysEnrichment.aggregate stub added — caught by running the full suite, not assumed",
                      "41 new/updated tests, all passing: censysProvider.test.js (15), censysEnrichmentRouteAuthorization.test.js (14, full route->controller->service->provider chain with a faked globalThis.fetch), censysLiveSmoke.test.js (3), phase8bCensysProviderEvidence.test.js (7), phase7RateLimiting.test.js +2 (shared-budget and own-budget proofs)",
                      "Docs: README External providers table + smoke command, docs/ai/SECURITY.md 'Censys - the second live provider (Phase 8B)' subsection, backend/.env.example CENSYS_* block",
                      "Fixed the CI 'migration count and order match the reviewed history' guard, which pins an exact migration-directory list on purpose — added this ticket's migration folder name to it (.github/workflows/ci.yml), per the guard's own instruction to update it in the same PR",
                      "Pushed feat/phase-8b-censys-provider (commits 4843c0b, fd297ef) and watched GitHub Actions run 31155494175 to a green conclusion (all required jobs; Mutation and concurrency gates remains manual by design)"
                  ],
    "validation":  {
                       "backend":  "PASS — 2899 passed / 177 skipped (115 files passed, 16 skipped), up from the 2858 baseline by exactly the 41 new tests this ticket adds; no regressions",
                       "prisma_migrate_from_zero":  "PASS — all 19 migrations (18 pre-existing + this ticket's) applied cleanly on a disposable PostgreSQL 16 container from zero",
                       "prisma_validate":  "PASS — schema valid",
                       "prisma_drift":  "PASS — `prisma migrate diff --exit-code` against the same live database reports zero difference after applying this ticket's migration",
                       "frontend":  "PASS — 143/143 unit tests unchanged, no regressions from the Settings.jsx/DashboardSections.jsx edits",
                       "route_census":  "PASS — phase7RouteCensus.test.js (the structural walk requiring every mounted route to authenticate + enforce a capability) covers both new Censys routes automatically, no exception-list edit needed",
                       "rate_limit_sharing":  "PASS — new phase7RateLimiting.test.js case proves the Censys route shares the SAME providerRateLimiter budget as IOC enrichment (2 IOC calls + 1 Censys call = 429), and that the Censys route alone also bounds its own rate",
                       "secret_hygiene":  "PASS — backend/.env never opened this session; new tests assert injected fake CENSYS_API_ID/CENSYS_API_SECRET values never appear in any response body, audit row, or smoke-script output; no live Censys call was made",
                       "no_live_calls_in_tests":  "PASS — every censysProvider.test.js case injects its own fetchImpl; censysEnrichmentRouteAuthorization.test.js fakes globalThis.fetch for the full route chain; censysLiveSmoke.test.js never sets the opt-in env var in a way that reaches the network",
                       "git_status":  "Clean diff, explicit paths staged only; docs/codex/ untouched throughout",
                       "ci_workflow":  "PASS — GitHub Actions run 31155494175 at commit fd297ef, conclusion success: Secrets/artifact scan, Backend tests, Frontend lint/tests/build, Prisma schema/migration history, Core evaluators, Browser suite (Chromium) all green; Mutation and concurrency gates (manual) intentionally not triggered. First push (31155284518) failed only the frozen migration-history guard, fixed in the same PR (see completed[])."
                   },
    "known_issues":  [
                         "The manual Censys live smoke was NOT executed against the real Censys API this session (not authorized); implemented and unit-tested with a mocked fetch only.",
                         "Censys is exposed as a Finding-scoped, synchronous, human-triggered lookup only — there is no batch/queue worker for it (matches this phase's explicit 'no queues/schedulers' scope), so it cannot be bulk-run the way IOC/vulnerability enrichment batches can.",
                         "docs/codex/ remains untracked and untouched, per the one-writer boundary."
                     ],
    "next_action":  "Run the remaining local gate items (already substantially done), commit with explicit paths, push feat/phase-8b-censys-provider, watch CI to green, release the writer lock.",
    "checkpoint_ref":  "HEAD",
    "updated_at":  "2026-08-07T00:00:00.0000000Z",
    "next_writer":  "unassigned"
}
