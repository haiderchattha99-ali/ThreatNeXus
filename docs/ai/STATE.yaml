{
    "schema_version":  1,
    "ticket":  "TNX-P8D-GREYNOISE-PROVIDER",
    "title":  "Phase 8D - GreyNoise live internet-noise/reputation provider integration",
    "status":  "in_progress",
    "owner":  "claude",
    "reviewer":  "codex",
    "branch":  "feat/phase-8d-greynoise-provider",
    "worktree":  "C:\\Users\\LENOVO\\Desktop\\ThreatNeXus",
    "starting_commit":  "88a2c48",
    "goal":  "Add exactly one new live provider, GreyNoise, for IP noise/reputation/context using the existing provider foundation and Censys/NVD patterns. No live calls in CI/tests, no secret leakage, provider rate-limit/audit/status support, optional manual smoke, docs/ai handoff, and green CI. Do not add Shodan, Netlas, Shadowserver, or live AI.",
    "scope":  {
                  "included":  [
                                   "GreyNoiseEnrichment Prisma model + additive-only migration 20260807110000_add_phase8d_greynoise_reputation_enrichment (own table, no change to any existing table - GreyNoise's noise/classification/actor data does not fit IocEnrichment's AbuseIPDB-shaped columns any more than Censys's exposure data did)",
                                   "backend/src/services/reputation/: greyNoiseTypes.js, greyNoiseConfig.js, greyNoiseProvider.js (Community API, `key` header auth, IPv4 only), greyNoiseExecutionService.js - all mirroring censysProvider.js/censysExecutionService.js's exact defensive shape",
                                   "backend/src/controllers/greyNoiseEnrichmentController.js + routes/greyNoiseEnrichmentRoutes.js, mounted at /api/findings (GET+POST .../enrichment/greynoise), reusing read:findings / trigger:finding-enrichment (no new capability) and the existing providerRateLimiter budget",
                                   "backend/src/scripts/greyNoiseLiveSmoke.js (npm run smoke:greynoise), opt-in via LIVE_GREYNOISE_SMOKE=1, one lookup against 1.1.1.1, never run in CI, NOT executed this session (not authorized)",
                                   "Dashboard: new sections.providers.reputation array (sibling to .exposure, not folded into it - GreyNoise is neither exposure nor the single ioc-reputation slot), operationalOverviewService.js wiring, one-line frontend additions in Settings.jsx and DashboardSections.jsx reusing the existing flattened provider-row rendering",
                                   "docker-compose.yml/docker-compose.offline.yml updated for GREYNOISE_API_KEY passthrough + offline blackhole host",
                                   "40 new/updated tests: greyNoiseProvider.test.js (16), greyNoiseEnrichmentRouteAuthorization.test.js (14), phase8dGreyNoiseProviderEvidence.test.js (6), 2 new phase7RateLimiting.test.js cases, operationalOverviewService.test.js + riskFactorPressure.test.js extended for the new reputation section. Backend suite: 2990 passed / 177 skipped (up from 2950 baseline), zero regressions",
                                   "Docs: README.md (provider table row), docs/ai/SECURITY.md (new Phase 8D section), backend/.env.example, docs/ai/HANDOFF.md, docs/ai/STATE.yaml, CI's frozen migration-history list"
                               ],
                  "excluded":  [
                                   "Shodan, Netlas, Shadowserver, VirusTotal, OTX, MISP integration",
                                   "Any live AI provider",
                                   "Scheduled/queued GreyNoise jobs - explicitly synchronous, human-triggered only",
                                   "IPv6 indicator support - no validator exists anywhere in this codebase to extend safely",
                                   "A unified cross-cutting provider registry spanning IOC/vulnerability/exposure/reputation - four separate, intentionally un-unified registries/tables by design",
                                   "Rewriting or weakening AbuseIPDB, NVD, or Censys - all three verified unaffected (registry-isolation tests)",
                                   "PR creation or merge into main"
                               ]
              },
    "completed":  [
                      "Verified the mandatory start gate: fetched origin, confirmed Phase 8C.1 already merged into origin/main via PR #13 (88a2c48) while local main was 4 commits stale; fast-forwarded local main, branched feat/phase-8d-greynoise-provider from the updated tip",
                      "Acquired the AI-team writer lock for TNX-P8D-GREYNOISE-PROVIDER directly (same F-drive start-task.ps1 schema-mismatch workaround as Phases 8C/8C.1)",
                      "Read the Censys adapter (Phase 8B) in full as the exact template before writing anything - censysTypes/Config/Provider/ExecutionService/Controller/Routes/LiveSmoke - plus providerRegistry.js and IocEnrichment's schema, to make the deliberate call that GreyNoise needs its own table (like Censys) rather than a bolt-on to IocEnrichment (like it would need to be to reuse AbuseIPDB's registry), because GreyNoise's classification/actor/noise shape doesn't fit either existing table",
                      "Designed the dashboard status wiring as a NEW sibling array (reputation) rather than reusing exposureProviders, on the reasoning that GreyNoise is a distinct provider domain from exposure/attack-surface - matches the same per-domain-array pattern Censys itself established, applied consistently rather than taking the smaller-diff-but-less-honest shortcut of mislabeling GreyNoise as exposure",
                      "Implemented the full backend surface: types/config/provider/execution-service/controller/routes/live-smoke, env.js wiring, Prisma model+migration, docker-compose env passthrough + offline blackhole, dashboard status section",
                      "Wrote 40 new/updated tests; all 16 provider tests and all 14 route-authorization tests passed on the first real run",
                      "Full backend suite run found 2 real regressions from adding the new `reputation` section to buildProvidersSection's output: operationalOverviewService.test.js's roll-up-total assertion didn't account for the new array (fixed: added + reputation.length), and riskFactorPressure.test.js's separate Prisma fake was missing a greyNoiseEnrichment.aggregate stub entirely, causing the whole providers section to fail (fixed: added the stub, mirroring the existing censysEnrichment one). Re-ran the full suite: 2990 passed / 177 skipped, zero regressions",
                      "Updated README.md, docs/ai/SECURITY.md, backend/.env.example, docs/ai/HANDOFF.md, docs/ai/STATE.yaml, and CI's frozen migration-history list"
                  ],
    "validation":  {
                       "backend":  "PASS - 2990 passed / 177 skipped, zero regressions (2 pre-existing test fixtures updated to include the new reputation section, not weakened)",
                       "prisma_validate":  "PASS",
                       "prisma_generate":  "PASS",
                       "secret_hygiene":  "PASS - no .env file read/printed/committed; no real GreyNoise credential ever introduced; GREYNOISE_API_KEY redaction proven in operationalOverviewService.test.js and greyNoiseEnrichmentRouteAuthorization.test.js",
                       "no_live_calls_in_tests":  "PASS - every test injects its own fetchImpl or fakes globalThis.fetch; phase8dGreyNoiseProviderEvidence.test.js proves the live-smoke script refuses to run without LIVE_GREYNOISE_SMOKE=1",
                       "live_smoke":  "NOT RUN - not authorized this session, per explicit instruction",
                       "git_status":  "Clean diff, explicit paths staged only; docs/codex/ untouched throughout"
                   },
    "known_issues":  [
                         "Manual live GreyNoise smoke not run - awaiting explicit user authorization.",
                         "F-drive start-task.ps1 throws against this repo's STATE.yaml schema. Worked around, not fixed - same known gap as Phases 8C/8C.1.",
                         "docs/codex/ remains untracked and untouched, per the one-writer boundary."
                     ],
    "next_action":  "Push feat/phase-8d-greynoise-provider and watch CI (its Postgres service is the first real migrate-from-zero + evaluator proof).",
    "checkpoint_ref":  "HEAD",
    "updated_at":  "2026-08-07T12:20:00.0000000Z",
    "next_writer":  "claude"
}
