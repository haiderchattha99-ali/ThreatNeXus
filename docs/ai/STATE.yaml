{
    "schema_version": 1,
    "ticket": "TNX-P10A1-ENRICHMENT-ORCHESTRATION-FOUNDATION",
    "title": "Phase 10A-1 - inert enrichment-orchestration foundation",
    "status": "complete",
    "owner": "claude",
    "reviewer": "codex",
    "branch": "feat/phase-10a1-enrichment-orchestration-foundation",
    "worktree": "F:\\AI-Worktrees\\ThreatNeXus\\phase-10a1",
    "starting_commit": "3638a3983ec7e45e7dfc70cb9583e7b6a5cd8912",
    "goal": "Implement the inert Phase 10A-1 enrichment-orchestration foundation: one additive migration (5 tables, 9 enums, 7 CHECK constraints), subject model, applicability router, run/item/job services, reconciliation service (callable, unscheduled), async run/summary/usage APIs, default-off configuration, ingestion integration and backend tests. Zero provider calls, zero quota reservations, zero attempt/usage rows, no worker, no frontend change.",
    "current_work": "",
    "files_changed": [
        "backend/prisma/schema.prisma",
        "backend/prisma/migrations/20260811162611_add_phase10a1_enrichment_orchestration/migration.sql",
        "backend/src/services/enrichmentOrchestration/ (10 new modules)",
        "backend/src/controllers/enrichmentOrchestrationController.js",
        "backend/src/routes/enrichmentOrchestrationRoutes.js",
        "backend/src/routes/enrichmentUsageRoutes.js",
        "backend/src/app.js",
        "backend/src/config/env.js",
        "backend/src/services/ingestion/reportIngestionService.js",
        "backend/.env.example",
        ".github/workflows/ci.yml (frozen migration list -> 24)",
        "backend/tests/unit/ (6 new suites)",
        "backend/tests/integration/ (3 new real-PostgreSQL suites)",
        "README.md, docs/PROVIDER_GUIDE.md, docs/DEPLOYMENT.md, docs/ai/SECURITY.md, docs/ai/HANDOFF.md, docs/ai/STATE.yaml"
    ],
    "root_cause": "frontend/src/pages/Upload.jsx called threatService.uploadCSV(), which posts to the LEGACY POST /api/threats/upload route. That route runs threatController.uploadThreatCSV and writes standalone Threat rows: no RawReport, no RawReportRow evidence, no Finding, no FindingOccurrence, no ownership resolution, no enrichment scheduling, no risk recalculation and no report.ingestion.* audit event. The page then rendered a 'Report processed' heading from that route's 'N threat(s) added successfully' message, so an upload reported success while the Findings workspace, the occurrence history and the operational overview received nothing. Upload.jsx already read result.findingCounts - a field only the CANONICAL controller returns - so the lifecycle chips were dead code against the legacy response body: the page had been written for the right contract and wired to the wrong one. The old Upload.test.jsx mocked threatService.uploadCSV and asserted it was called, which is structurally incapable of noticing the wrong route, and the old E2E asserted only that the heading 'Report processed' appeared.",
    "final_contract": "POST /api/reports/upload (frontend: reportIngestionService.uploadReport). multipart/form-data carrying exactly one field, 'file'. source (SYNTHETIC_UPLOAD), reportType (ACCESSIBLE_RDP) and schemaVersion (accessible-rdp.synthetic.v1) remain server-decided in reportIngestionController.js and are read from no request field. Outcomes are read from the status the controller assigns, one per closed INGESTION_OUTCOMES member: 201 PROCESSED, 200 DUPLICATE_COMPLETED, 409 DUPLICATE_IN_PROGRESS, 400 REJECTED, 422 UNPROCESSABLE_NO_VALID_ROWS, 500 FAILED, plus 413/429/403 and a no-response transport failure classified separately as refusals that never entered the pipeline. Backend authentication, RBAC (ingest:reports), multer ordering, upload limits, the uploadRateLimiter mount and every ingestion semantic are unchanged. The legacy /api/threats/upload route is left mounted and untouched.",
    "scope": {
        "included": [
            "One additive migration: 5 tables, 9 enums, 7 CHECK constraints. No existing column altered.",
            "EnrichmentSubjectType + provider/subject compatibility (5 IPv4 providers, nvd CVE-only), canonicalization before hashing",
            "queryIdentityHash (work identity), requestScopeHash (ask identity), bounded Idempotency-Key handling (max 128 UTF-8 bytes, no control chars, SHA-256 digest only)",
            "Applicability router with explicit precedence; force bypasses freshness only",
            "Run/item/lookup-job repository + services, run-state recomputation, shared activeLookupKey dedup, read-only delegate links",
            "Reconciliation service - callable in tests, scheduled by nothing",
            "Async run/summary APIs (202 Accepted) and truthful usage API with PHASE_10_RESERVATIONS / PARTIAL / reservationsActive=false / excludedPaths",
            "AUTO_ENRICHMENT_ENABLED=false, ENRICHMENT_WORKER_ENABLED=false, every automatic budget 0, manual budgets unlimited-but-validated",
            "Ingestion integration behind the switch + one additive autoEnrichment response block",
            "Audit events for the inert milestone (counts and closed codes only)"
        ],
        "excluded": [
            "worker.js or any worker loop; provider executors, factories, lookups, fetch calls - a static inertness test proves the package imports none of them",
            "Quota reservation SQL/service; ProviderLookupAttempt rows; ProviderDailyUsage rows - all zero by design",
            "MANUAL_DIRECT wiring; targeted IOC runner changes; vulnerability runner changes; automatic NVD/EPSS/KEV execution",
            "Frontend changes (git status frontend/ empty), cancellation, Risk v1 changes, live provider smoke tests",
            "PR creation and any merge to main - explicitly out of scope",
            "backend/.env - never opened, read, printed or referenced; only .env.example consulted"
        ]
    },
    "completed": [
        "Mandatory start gate: read project AGENTS.md/CLAUDE.md, the F-drive global AGENTS.md/CLAUDE.md role files, docs/ai/{STATE.yaml,HANDOFF.md,SECURITY.md} and the threatnexus-build-guard skill before writing anything",
        "Fetched origin: local main and origin/main both at c3f8a4b (0/0 divergence); the primary checkout was on docs/phase-9c-pkcert-technical-dossier at 5fe93d2 with six modified tracked files and ~35 untracked paths, all foreign to this ticket; no .ai-team/WRITER_LOCK.json existed anywhere, so no other writer held the repository",
        "Created an isolated worktree F:\\AI-Worktrees\\ThreatNeXus\\ingestion-repair on fix/analyst-report-ingestion-contract from origin/main (c3f8a4b), per the ticket's dirty-checkout rule. The primary checkout was never touched again",
        "FIXED (rather than worked around) the F-drive writer-lock gap every handoff since Phase 8B has recorded: start-task.ps1 and checkpoint-task.ps1 assign $state.current_work, $state.files_changed, $state.validation.passing and $state.validation.failing, none of which existed in this repository's STATE.yaml, so both threw 'property cannot be found on this object'. Added the four keys, committed that alone first (4c60ed0) to satisfy Assert-TeamClean, then acquired the lock successfully - the first real .ai-team/WRITER_LOCK.json this repository has ever held",
        "Reproduced the defect from source before changing anything, and confirmed every claim in the ticket: Upload.jsx:38 called threatService.uploadCSV; api.js:114 posted to /threats/upload; threatRoutes.js mounted uploadThreatCSV there; threatController returned '{added} threat(s) added successfully'. Also found a detail the ticket did not state - Upload.jsx already rendered result.findingCounts, a field only the canonical controller sends, so the lifecycle chips could never have appeared",
        "Verified the canonical response shape by reading reportIngestionController.js and reportIngestionService.js directly rather than guessing: the body carries success/message/report, findingCounts ONLY on PROCESSED, and reason ONLY on REJECTED/UNPROCESSABLE_NO_VALID_ROWS. The controller does NOT return an `outcome` field, but maps each closed outcome to exactly one distinct status - so the frontend reads the status as the outcome, and no backend contract change was needed",
        "RED-CHECKED the new unit suite: temporarily re-pointed Upload.jsx at threatService.uploadCSV and confirmed the contract test fails with 'Expected: \"/reports/upload\", Received: \"/threats/upload\"', then restored the fix and re-ran green. A test that cannot fail on the regression it exists to catch would have repeated the original mistake",
        "Stood up a clean isolated Docker stack on conflict-free ports (postgres 55432, backend 5100, Playwright preview 4273) under its own compose project tnx-ingestion, with the port/CORS override kept in the session scratchpad and never committed. Applied 23 migrations from zero, seeded via the approved npm run seed:users + seed:demo workflow (11 findings)",
        "Ran the corrected upload in a real Chromium browser against that stack and asserted the request URL on the wire, the 201 outcome, the persisted row counters, the created Finding's own occurrence evidence, and the dashboard delta - then corroborated all of it directly in PostgreSQL",
        "Found and fixed a real test-harness defect during the live run: the dashboard KPI is a GSAP count-up, and reading its innerText returned 10 for a value of 13 while the digits were still climbing. The authoritative figure is the data-count-to attribute Metric.jsx writes straight from metric.value; the helper now reads that, which also yields a correct null for RESTRICTED/UNAVAILABLE tiles instead of a coerced zero",
        "Tore the stack down with `down -v`, rebuilt from zero and re-seeded before running the FULL browser suite, so the gate ran against a pristine database rather than one carrying this session's own probe data",
        "Ran proportionate complete gates: frontend lint, 169 unit tests, production build, 55/55 Chromium Playwright; backend prisma format/validate/generate, migrate deploy from zero, migrate diff drift check (exit 0), focused ingestion tests (65), and the complete backend suite (3071 passed / 177 skipped - the documented Phase 8F baseline, unchanged, as expected for a frontend-only change)",
        "Reverted an unintended `prisma format` line-ending rewrite of backend/prisma/schema.prisma (CRLF->LF, zero content change) so this ticket touches no backend file at all",
        "Ran CI's own security scans: no committed .env, no credential-shaped literals, no tracked dist/node_modules, no secret-shaped values in frontend/dist. Confirmed every provider key is empty in the verification stack and AI_ENABLED=false, so no live provider or AI call was possible at any point",
        "Staged 7 explicit ticket-owned paths only (never git add -A), after a git add --dry-run review that caught STATE.yaml wanting to ride along",
        "Independent Codex review (0 critical, 0 high, 1 medium): the dashboard-delta E2E proof silently skipped the before/after comparison when a KPI tile was null, which could mask a real RESTRICTED/UNAVAILABLE regression as a pass. Reacquired the writer lock, added requireKpiValue() (asserts non-null and finite, with a diagnostic message) in findingsUpload.spec.js, replaced both call sites and the conditional delta check with hard assertions. Rebuilt the isolated stack from zero, re-ran the targeted spec (5/5) and the complete Chromium suite (55/55) against a pristine seed, plus frontend lint/unit/build - all green. No production code touched"
    ],
    "validation": {
        "prisma": "format + validate pass; migrate deploy from zero applies 24/24 on a fresh database; migrate diff --exit-code reports no difference (exit 0)",
        "schema_objects": "5 tables, 9 enums (ProviderLookupJobState 12 values incl. SKIPPED_BUDGET, RunItemDecision 8) and 7 CHECK constraints verified by direct pg_catalog query on the migrated database",
        "constraints": "9 real-PostgreSQL tests proving 8 independent rejections across the 7 constraints, each asserting the constraint NAME in the error so an unrelated NOT NULL or FK cannot be miscounted as proof",
        "orchestration": "9 real-PostgreSQL tests: T-23 (an AbuseIPDB-scoped run does not suppress a manual Censys-scoped run; 2 distinct runs, Censys has its own ELIGIBLE item and PENDING job, zero attempts), concurrent identical requests collapse to 1 run via separate connection pools, different scopes create different runs, 2 Findings on 1 subject share 1 job, policy skips create no jobs, automatic budget 0 creates no job and no reservation, 3 verified CVEs create 3 NVD jobs, removed or provider-text CVEs create none, audit payload carries counts not subjects or hashes",
        "default_off": "3 real-PostgreSQL tests: switch off = zero Phase-10 rows + legacy IocEnrichment still created + all pre-existing response fields intact + zero orchestration audit events; switch on = durable records, jobs non-terminal and never queried, zero attempts, zero usage; identical re-upload creates no second run",
        "unit": "101 tests across 6 suites, including a static inertness gate that fails on any forbidden import, fetch or lookup call, attempt or usage write, raw SQL, timer, or worker in the package",
        "full_suite": "3368 passed, 2 skipped, 0 failed (154 files) on a fresh database at --maxWorkers=3, with every provider key empty",
        "evaluators": "eval:phase1 PASS, eval:risk PASS (19/19 - Risk v1 locked contract and fingerprint unchanged), eval:phase2 PASS (22 scenarios / 112 assertions), eval:phase3 PASS (12/151), eval:vulnerability PASS (41/992), eval:phase4 PASS (14/151), eval:phase5 PASS (14/150), eval:phase6.3 PASS (13/108), eval:phase7 PASS (8/35)",
        "risk_unchanged": "RISK_CONFIGURATION_VERSION v1.0.0 and RISK_ALGORITHM_VERSION risk-additive-bucketed-v1 unchanged; backend/src/services/risk/ untouched per git status; eval:risk re-proves the fingerprint",
        "secret_scan": "CI's own credential-shaped-literal pattern set run repo-wide including the new files: clean. No tracked .env. No provider key variable is named anywhere outside enrichmentOrchestrationConfig.js, asserted by a unit test",
        "frontend": "untouched - git status frontend/ is empty",
        "no_real_env_read": "PASS - backend/.env never opened, read, printed or referenced"
    },
    "known_issues": [
        "MANDATORY PRE-10A2 BLOCKER: the existing IOC runner owns claimPendingJob and calls provider.lookup() internally. A targeted runner/hook contract must be designed AND independently reviewed before 10A-2 - see docs/ai/HANDOFF.md for the seven guarantees it must provide.",
        "SKIPPED_EXECUTION_UNAVAILABLE, MANUAL_DIRECT, ProviderLookupAttempt and ProviderDailyUsage are defined and constraint-proven but deliberately unused or empty in 10A-1 - they exist so the accounting contract is provable before any code can spend quota.",
        "ENRICHMENT_WORKER_ENABLED is declared and validated but consumed by nothing; there is no worker to enable.",
        "Freshness is read one query per routed target (bounded by provider scope plus one per verified CVE). Marked with a ponytail: comment naming the batching upgrade path.",
        "The Phase 10 Plan v2 and v2.1 documents were NOT available in the implementing session (fresh context; they exist in no file on disk or on the F drive). The data model was recovered from the prior session's committed schema.prisma, and the async run/summary API shape plus the additive upload-response field names were derived from repo conventions with the user's explicit approval. If v2/v2.1 names those fields differently, reconcile the NAMES - the behaviour and contracts should already match.",
        "Local full-suite runs at default parallelism produce 5s-timeout failures in unrelated pre-existing suites (caseWorkflowConcurrency, vulnerabilityPacketBApplication, vulnerabilityReleaseWorkflow). Proven unrelated: they import no changed module, they pass in isolation, and the same failures still occur with every Phase-10 file excluded. A fresh database at --maxWorkers=3 is fully green.",
        "No independent reviewer has seen this branch yet - per CLAUDE.md it must not be treated as final."
    ],
    "next_action": "Codex independent review of feat/phase-10a1-enrichment-orchestration-foundation. Do NOT open a PR, do NOT merge main, do NOT begin 10A-2. After review passes, design the targeted IOC runner/hook contract recorded in docs/ai/HANDOFF.md and have that design independently reviewed before writing any 10A-2 execution code.",
    "checkpoint_ref": "3638a3983ec7e45e7dfc70cb9583e7b6a5cd8912",
    "updated_at": "2026-08-11T22:05:22.059Z",
    "next_writer": "codex"
}
