{
    "schema_version":  1,
    "ticket":  "TNX-P10A2-LIVE-ENRICHMENT-EXECUTION",
    "title":  "Phase 10A-2 - live enrichment execution (worker, quota ledger, targeted IOC hook)",
    "status":  "awaiting_final_review",
    "owner":  "claude",
    "reviewer":  "codex",
    "branch":  "feat/phase-10a2-live-enrichment-execution",
    "worktree":  "F:\\AI-Worktrees\\ThreatNeXus\\phase-10a2",
    "starting_commit":  "4879097",
    "goal":  "Make the intent Phase 10A-1 recorded EXECUTABLE, default-off, for Censys/GreyNoise/Shodan/Netlas directly and AbuseIPDB through a targeted hook into the canonical IOC workflow. NVD stays on the ADMIN vulnerability batch. Provide the seven binding runner guarantees from the pre-10A-2 blocker in HANDOFF.md, with truthful crash semantics that never claim exactly-once network delivery.",
    "binding_contract":  "docs/ai/PHASE-10A2-RUNNER-CONTRACT.md (v3) - written BEFORE any execution code, reviewed independently TWICE before implementation began, and amended after each. Section 18 is the finding-by-finding resolution table; section 19 lists the honest gaps. Code that disagrees with it is wrong.",
    "scope":  {
                  "included":  [
                                   "enrichmentWorker.js - tick loop (stale-claim recovery, delegate reconciliation BEFORE the sweep, stale-attempt sweep, run-state reconciliation, direct pass, targeted pass), startup behind the switch, draining shutdown",
                                   "enrichmentQuotaService.js - atomic daily reservation AND ProviderLookupAttempt creation in ONE interactive transaction; exactly-once finalization guarded on state IN (RESERVED, IN_FLIGHT); the non-expiring CONTACT_SENTINEL",
                                   "enrichmentDirectExecutionService.js - reuses each provider's existing buildProvider/toPersistedRow/evidence table; ONE post-call transaction (evidence + finalize + terminalize + FK); the worker's own end-to-end lookup bound",
                                   "enrichmentTargetedIocService.js - claims ONLY the linked IocEnrichment row, never leases the Phase-10 job",
                                   "enrichmentRunner.runTargetedEnrichmentJob + two hooks (authorize, beforeLookup); runEnrichmentBatch and claimPendingJob UNCHANGED",
                                   "iocEnrichmentRepository.holdContactedJob + deadLetterUnleasedJob (additive; the ADMIN batch calls neither)",
                                   "Reconciliation AMENDED: RATE_LIMITED -> FAILED, DEAD_LETTER -> DEAD_LETTER, guarded transition, refreshRunState per linked run",
                                   "Migration 25 (additive): AttemptOutcome gains PROVIDER_REJECTED; CHECK promoting the delegate-linkage invariant. CI frozen list updated in the same commit",
                                   "executionState NOT_IMPLEMENTED retired for ACTIVE across the vocabulary, the read service and its pinned tests (D-P10A2-03)",
                                   "Worker runtime config with CROSS-FIELD validation (lease >= lookupMax+30s, stale >= lookupMax+60s), validated even when the worker is disabled",
                                   "docker-compose passthrough of every Phase-10A-2 control and every provider budget, all defaulting safe",
                                   "Tiered static execution gate replacing the 10A-1 inertness gate",
                                   "21 real-PostgreSQL core-guarantee tests"
                               ],
                  "excluded":  [
                                   "The full 47-case test matrix. Scoped down to CORE guarantees by explicit user decision. Per-status permutations (404/429/5xx/timeout/malformed/network for each of five providers) are NOT covered end to end; status mapping is covered at unit level on resolveAttemptOutcome",
                                   "Any change to the five shared provider modules. The worker bounds lookup() at its own call site instead (D-P10A2-09), because changing their timeout semantics would alter merged behaviour on the ADMIN and expert paths and risk guarantee 6",
                                   "VulnerabilityEnrichmentJob worker-eligibility. NVD still requires the ADMIN vulnerability batch",
                                   "Any new HTTP route, frontend change, or Risk v1 change - backend/src/services/risk/ has an EMPTY diff",
                                   "Real provider smoke tests and real API credentials - never used, never requested",
                                   "backend/.env - never opened, read, printed or referenced; only .env.example consulted",
                                   "The primary checkout C:\\Users\\LENOVO\\Desktop\\ThreatNeXus - never touched, never switched, never stashed; every git command used git -C",
                                   "PR creation and any merge to main - explicitly out of scope"
                               ]
              },
    "codex_design_review":  {
                                "pass_1":  "NOT READY - 12 P1, 1 P2, 1 P3, no P0. ALL FOURTEEN verified against the repository and ALL substantiated; none rejected as invented. Contract rewritten to v2.",
                                "pass_2_bounded_recheck":  "NOT READY - 10 of 14 resolved; F1, F2, F9, F12 narrowed to concrete corrections, all applied in v3. Codex confirmed no new blockers introduced by v2.",
                                "latent_10a1_defects_found":  [
                                                                  "IOC_STATUS_TO_JOB_STATE omitted the TERMINAL statuses RATE_LIMITED and DEAD_LETTER. Its comment claimed a rate-limited delegate is 'still working', which is false: TERMINAL_STATUSES is every status except PENDING and resolveEnrichmentRetry COMPLETEs it. Such a delegate pinned its Phase-10 job in WAITING_ON_DELEGATE and held activeLookupKey FOREVER, permanently blocking every future ask about that subject. reconcileDelegatedJobs also never called refreshRunState and used an unguarded updateJob.",
                                                                  "resolveEnrichmentRetry returns COMPLETE for EVERY terminal status, so the v1 design's RETRY_WAIT for RATE_LIMITED was unimplementable without changing the branch the ADMIN batch shares.",
                                                                  "AttemptOutcome had no value for PROVIDER_REJECTED, which every provider emits for non-auth 3xx/4xx - a Censys HTTP 400 could only be recorded as SERVER_ERROR or TRANSPORT_ERROR, both false.",
                                                                  "NO provider timeout bounds lookup(): every provider clears its timeout when response HEADERS arrive and reads the body afterwards (censysProvider.js:139/182, greyNoiseProvider.js:117/160). A stalled body read runs unbounded, so the stale sweep could resolve an attempt still executing."
                                                              ],
                                "duplicate_charge_window_found":  "The IOC lease (120s) expires long before any stale sweep, so the ADMIN batch could reclaim and RE-CALL a row whose first request was still unresolved. v1 had no defence; v2's duration-based guard was also wrong because an ordering of durations bounds durations, not recovery SCHEDULING. Closed in v3 by a NON-EXPIRING sentinel written into nextAttemptAt, reusing a gate claimPendingJob already applies - with claimPendingJob itself unmodified."
                            },
    "validation":  {
                       "prisma":  "format + validate + generate pass; migrate deploy from zero applies 25/25 on a fresh database; migrate diff --exit-code reports no difference (exit 0)",
                       "new_suite":  "tests/integration/phase10a2Execution.test.js - 21/21 real-PostgreSQL cases over the seven guarantees, quota atomicity, claim races, ordering, ambiguity and targeted selection",
                       "unit":  "2427 passed across 108 files, including the re-tiered execution gate (12 cases)",
                       "full_suite":  "3428 passed, 54 skipped, 1 failed against real PostgreSQL at --maxWorkers=3. The single failure is vulnerabilityCoreConcurrency, which passes 19/19 IN ISOLATION and exercises only vulnerability-queue code this branch never modified - the documented local contention flake class",
                       "evaluators":  "ALL NINE PASS - eval:phase1, eval:risk, eval:phase2, eval:phase3, eval:phase4, eval:phase5, eval:phase6.3, eval:phase7, eval:vulnerability - run with EVAL_DATABASE_URL pointed at a separate disposable database",
                       "risk_unchanged":  "backend/src/services/risk/ has an EMPTY diff against 4879097; eval:risk re-proves the fingerprint",
                       "frontend":  "lint (pre-existing warnings only), 169 tests passed, production build clean, bundle carries no secret-shaped literal",
                       "secret_scan":  "CI's three hygiene checks run locally over the tracked tree: no committed .env, no credential-shaped literal, no committed build output. Clean. git diff --check clean (CRLF warnings only)",
                       "compose":  "docker compose config valid",
                       "rehearsal_worker_disabled":  "PASS - plain docker compose up: stack healthy, NO worker log line, and 0 ProviderLookupAttempt / 0 ProviderDailyUsage / 0 ProviderLookupJob rows",
                       "rehearsal_worker_enabled":  "PASS - worker ON with POSITIVE budgets but EMPTY provider keys: the worker starts and logs it, backend stays healthy, and 0 usage rows and 0 attempts are written because a missing credential is refused BEFORE any reservation. Proves the switch, the truthful NOT_CONFIGURED path and the no-spend guarantee together, with no real key ever existing",
                       "no_live_provider_calls":  "PASS - direct providers driven through an injected fetchImpl (real provider parsing, no network); eval:phase7 replaces fetch with a throwing counter and asserts zero; CI sets every provider key to ''",
                       "no_real_env_read":  "PASS - backend/.env never opened, read, printed or referenced",
                       "primary_checkout":  "UNTOUCHED - C:\\Users\\LENOVO\\Desktop\\ThreatNeXus verified still at 5fe93d2 on docs/phase-9c-pkcert-technical-dossier with its Phase 9 work intact. Every git command in this session used git -C, never cd <path> && git",
                       "ci":  "pending - branch not yet pushed at the time of writing"
                   },
    "known_issues":  [
                         "A contacted IocEnrichment row can be held indefinitely if a worker dies after contact and no worker ever runs again. DELIBERATE: freezing one row is safer than double-calling a paid third party when we cannot establish what happened. Visible, and self-resolves when a worker next runs.",
                         "The pre-existing unbounded body read is NOT fixed for the ADMIN batch or the four synchronous expert endpoints. Every provider clears its timeout at the header boundary. Phase 10A-2 bounds only its own call site (D-P10A2-09). Recommended for its own ticket under its own review, because it changes shared merged code.",
                         "RETRY_WAIT is now an unused ProviderLookupJobState value (D-P10A2-06). Kept rather than removed: removing an enum value is a destructive migration.",
                         "No push wakeup - recorded work is discovered on the next poll, so worst-case latency is one ENRICHMENT_WORKER_POLL_INTERVAL_MS.",
                         "One provider call in flight per worker process. Throughput scales by running more processes, which the database-backed claim already makes safe.",
                         "The per-status permutation matrix was scoped out by explicit user decision - see scope.excluded.",
                         "vulnerabilityCoreConcurrency and vulnerabilityReleaseWorkflow are locally contention-flaky at full parallelism. Both pass in isolation and neither imports changed code."
                     ],
    "next_action":  "Await the final independent Codex implementation review verdict, resolve any P0/P1, push the branch, wait for CI at the final tip, then release the Claude writer lease. Do NOT open a PR, do NOT merge main, do NOT start Phase 10B.",
    "checkpoint_ref":  "b890353",
    "updated_at":  "2026-08-12T22:45:00.0000000Z",
    "current_work":  "Stage 6 delivery - documentation updated (README, DEPLOYMENT, OPERATIONS_RUNBOOK, PROVIDER_GUIDE, TESTING_AND_CI, SECURITY, DECISIONS, STATE, HANDOFF); final Codex review in flight",
    "files_changed":  [
                          "docs/ai/PHASE-10A2-RUNNER-CONTRACT.md (new - the binding contract, v3)",
                          "docs/ai/DECISIONS.md (D-P10A2-01..09)",
                          "backend/src/services/enrichmentOrchestration/enrichmentWorker.js (new)",
                          "backend/src/services/enrichmentOrchestration/enrichmentQuotaService.js (new)",
                          "backend/src/services/enrichmentOrchestration/enrichmentDirectExecutionService.js (new)",
                          "backend/src/services/enrichmentOrchestration/enrichmentTargetedIocService.js (new)",
                          "backend/src/services/enrichmentOrchestration/enrichmentOrchestrationRepository.js",
                          "backend/src/services/enrichmentOrchestration/enrichmentReconciliationService.js",
                          "backend/src/services/enrichmentOrchestration/enrichmentOrchestrationConfig.js",
                          "backend/src/services/enrichmentOrchestration/enrichmentDecisionCodes.js",
                          "backend/src/services/enrichmentOrchestration/enrichmentRunReadService.js",
                          "backend/src/services/enrichment/enrichmentRunner.js",
                          "backend/src/services/enrichment/enrichmentRunnerTypes.js",
                          "backend/src/services/enrichment/iocEnrichmentRepository.js",
                          "backend/src/services/enrichment/iocEnrichmentCacheRules.js",
                          "backend/src/config/env.js",
                          "backend/server.js",
                          "backend/prisma/schema.prisma",
                          "backend/prisma/migrations/20260812190000_add_phase10a2_execution_constraints/migration.sql (new)",
                          "backend/tests/integration/phase10a2Execution.test.js (new)",
                          "backend/tests/unit/enrichmentOrchestrationInertness.test.js (re-tiered)",
                          "backend/tests/unit/enrichmentRunServices.test.js (two pinned-defect corrections)",
                          "backend/.env.example, docker-compose.yml, .github/workflows/ci.yml",
                          "README.md, docs/DEPLOYMENT.md, docs/OPERATIONS_RUNBOOK.md, docs/PROVIDER_GUIDE.md, docs/TESTING_AND_CI.md, docs/ai/SECURITY.md, docs/ai/STATE.yaml, docs/ai/HANDOFF.md"
                      ],
    "next_writer":  "codex"
}
